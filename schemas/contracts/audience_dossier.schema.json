{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mc/contracts/audience_dossier/v0.1",
  "title": "MC Product-Studio Contract — Audience Dossier (v0.1)",
  "description": "The deep audience layer that FEEDS the message_brief. Segment-level, source-attributed, confidence-scored, NO PII. The most upstream artifact in the chain.",
  "version": "v0.1",
  "precedence_note": "v0.1 ranks are revisable post-pilot (global integer rank; β EVT-sp20260530-001-before-plan-beta-001).",
  "contract": {
    "owner_domain": "research_insight",
    "consumers": ["message_brief"],
    "precedence": 20
  },
  "type": "object",
  "required": ["type", "id", "segment", "sources", "confidence", "emotional_needs"],
  "properties": {
    "type": { "const": "audience_dossier" },
    "id": { "type": "string", "minLength": 1 },
    "segment": { "type": "string", "minLength": 1, "description": "Named cohort — never 'everyone'." },
    "sources": {
      "type": "array",
      "minItems": 1,
      "description": "Source attribution (Amazon/Reddit/competitor/etc.). No PII; synthetic claims labelled.",
      "items": { "type": "object", "required": ["ref"], "properties": { "ref": { "type": "string" }, "synthetic": { "type": "boolean" } } }
    },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "emotional_needs": { "type": "array", "minItems": 1, "items": { "type": "string" }, "description": "The deepest emotional needs, not just functional jobs." },
    "pii_free": { "type": "boolean", "const": true, "description": "Invariant: dossiers carry no PII." }
  },
  "additionalProperties": true
}
