{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mc/contracts/conversion_brief/v0.1",
  "title": "MC Product-Studio Contract — Conversion Brief (v0.1)",
  "description": "The conversion hypothesis + objection handling for the converting artifact. Derives from the message_brief.",
  "version": "v0.1",
  "precedence_note": "v0.1 ranks are revisable post-pilot (global integer rank; β EVT-sp20260530-001-before-plan-beta-001).",
  "contract": {
    "owner_domain": "marketing",
    "consumers": ["design_brief", "converting_artifact"],
    "precedence": 40
  },
  "type": "object",
  "required": ["type", "id", "derived_from_message_brief", "conversion_hypothesis", "objections_handled"],
  "properties": {
    "type": { "const": "conversion_brief" },
    "id": { "type": "string", "minLength": 1 },
    "derived_from_message_brief": { "type": "string", "minLength": 1, "description": "id of the source message_brief (spine reference)." },
    "conversion_hypothesis": { "type": "string", "minLength": 1 },
    "objections_handled": { "type": "array", "minItems": 1, "items": { "type": "string" } }
  },
  "additionalProperties": true
}
