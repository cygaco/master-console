<!-- EPIC TRACKER — spec §22. Linked from ../../ROADMAP.md § Planned epics and ../../TRACKER.md header.
     States (§19): Planned | Ready | Active | Blocked | Paused | Review Needed | Completed | Cancelled | Superseded -->

# E-LIFECYCLE-001 — MC Mode-Lifecycle Enforcement, Sprint Discipline, Dispatch-Shape Parallelism & Provider Readiness

- **Epic label and number:** E-LIFECYCLE-001
- **Title:** MC Mode-Lifecycle Enforcement, Sprint Discipline, Dispatch-Shape Parallelism & Provider Readiness
- **Owner:** President Agent
- **Parent roadmap area:** [ROADMAP.md § Planned epics](../../ROADMAP.md) — the "dispatch-shape operating layer" theme (successor to E-SYSTEM-ORG-001).
- **Goal:** Make MC mode/team/dispatch correctness MECHANICAL, not memory-dependent — a single Mode-Lifecycle Registry as the source of truth, a fail-closed `mode:init:gate` that blocks work until initialization passes, mechanical project-scoped team teardown, governed parallelism, disciplined turbo, tier-graded provider readiness, and a durable tracker-linked planning system.
- **Background:** MC keeps re-living one failure class — sprint/adhoc modes "forget" to stand up the persistent team, `/mode:` switches leak the old team, dispatch shape is chosen by recall not by a gate (root-caused as RT-2026-06-08-dispatch-class-rca; operator corrections "where's the team?"/"where's epsilon?" on 2026-06-06 + 2026-06-08). The cure is mechanical enforcement, not more prose. The full plan + the grounded Phase-0 investigation (6 read-only surfaces) live at [../../_planning/mc-lifecycle-plan.md](../../_planning/mc-lifecycle-plan.md); the source prompt is [../../_planning/ingest/mc-lifecycle.md](../../_planning/ingest/mc-lifecycle.md).
- **Scope:** (1) Mode-Lifecycle Registry (`.claude/agents/_org/mode-lifecycle.json`) — per-mode roster/bindings/tier/dispatch-profile/teardown, de-duplicating the 3 hardcoded sites; (2) Lifecycle-event registry + logger + coverage enforcer (2 real PreToolUse guards + ~23 virtual events to `paths.eventsFile`); (3) generalized fail-closed `mode:init:gate` + mode-switch preflight guard; (4) project-scoped persistent-team lifecycle manager (spawn/verify/kill/teardown/orphan/resume) + a SessionEnd hook backstop; (5) dispatch parallelism axis (under/over-dispatch detection) + coverage-gate live wiring; (6) turbo hardening (spend ceiling + safe commit/push/merge profile); (7) provider-tier (T1/T2/T3) readiness + preferred-tier config layered on the existing health stack; (8) `_planning` lifecycle integration + an `epic:` suite (design 10, build `/epic:plan`+`/epic:fold`) + canonicalized planning principles + an acceptance-criteria enforcement system.
- **Out of scope:** inventing new harness hook events (the set is closed at 8); rebuilding the dispatch matrix / provider health stack / tracker validator (all are EXTENDED, not replaced); the org/GTM "product-studio" expansion (the separate `_planning/plans/*` epic — the cast, not the stage); building all 10 `epic:` commands now; any live provider auth/funding/trust mutation during planning; replacing `/session:end`'s rich teardown (a mechanical backstop is added, not a replacement).
- **Current state:** Active
- **Percent completion:** ~94% — **CODE-COMPLETE: all 12 of 12 sprints landed, all gauntlet-clean; §22 #4 flip PARTIALLY DONE 2026-06-16 (2 keystone gates flipped to BLOCKING).** Wave 0 = S-LC-01 @76da472 + S-LC-02 @d23e212; Wave 1 (mechanical gates) = S-LC-03 + S-LC-04 + S-LC-05 @june-9; Wave 2 (dispatch + turbo) = S-LC-06 @975ed5c + S-LC-07 @95544ac (ADR-0011); Wave 3 (planning + epic-suite) = S-LC-08 @1a6d638 + S-LC-09 @db42f9a; Wave 4 (provider tier) = S-LC-10 @4a5e508; Wave 5 (AC enforcement + capstone) = S-LC-11 @d946f43 + S-LC-12 (capstone validation, α-led). The S-LC-12 capstone dry-run sim is GREEN with **0 defects** (21 regression suites + 8 live enforcers + 5 e2e pilot probes — see `_reports/E-LIFECYCLE-001-capstone-validation.md`). **State stays Active (not Completed)** because the remaining ~8% of the DoD is operator-gated/owed, NOT code: (1) the report-only → blocking flip is **PARTIALLY DONE 2026-06-16 (operator §22 #4 sign-off, verbatim "yes do the flip!")** — the 2 green-under-enforce keystone gates **S-LC-01 `mode-lifecycle-registry` + S-LC-02 `mode-lifecycle-hooks-coverage --enforce`** flipped to BLOCKING in `/scan:full` (verified green post-flip, validate 20/20); the other 3 lifecycle gates (`planning-principles` S-LC-08, `coverage-gate-scan` S-LC-06, `repo-role-single-source` ED-009) stay report-only **with documented reasons** — they are currently RED or carry a line-local-grep limitation, and you cannot safely flip a failing gate (it would brick `/scan:full`); their findings must be cleared first; (2) a **GPT 2nd-pass** on the gemini-only security lanes is owed (codex sustained-reap, §H). The **epic retro is DONE 2026-06-10** → `_reports/E-LIFECYCLE-001-epic-retro.md` (the RI-001-deferred close-out; learnings/friction/action-items synthesis — companion to the capstone validation report). The retro does NOT flip Completed/100%: it explicitly leaves the two operator-gated items (1)+(2) as the open work. A Wave-1-tail manifest gap (S-LC-05 `lifecycle.js` + `scripts/epic/` unshipped) was caught by ship-coverage + fixed (ASSET_DIRS).

## Definition of Done
- [ ] Mode-Lifecycle Registry is the SOLE source of truth (all readers resolve from it; the `mode-lifecycle-registry` validator is green; the DEFAULT-ON drift reconciled) — proven by a planted wrong-roster fixture failing.
- [ ] `mode:init:gate` blocks dispatch/tool-use until the 14-item init checklist passes, for every mode, fail-closed, idempotent — proven by the planted fixtures (no-team, wrong-team, duplicate-call, dispatch-before-init).
- [ ] Mode switch + `/session:end` **best-effort** + project-scopedly tear down teams (request-shutdown→TeamDelete→reconcile; no guaranteed kill of a live in-process teammate) — proven by a planted wrong-PROJECT team surviving a kill + an orphan being detected.
- [ ] The lifecycle-event registry fires in order with no secret leakage — proven by the coverage enforcer + planted out-of-order/missing-event/secret-in-payload fixtures.
- [ ] The dispatch parallelism axis flags under/over-dispatch AND `coverage-gate.js` has a live caller — proven by planted serial-when-safe + shared-file-collision fixtures.
- [ ] Turbo enforces its spend ceiling + safety profile (operator-approved default) — proven by planted over-spend + unsafe-push fixtures blocked.
- [ ] Provider-tier readiness checks the selected tier with a preferred-tier config — proven by a planted under-tier blocking the selected tier (value-free).
- [ ] `_planning` is a tracker-linked lifecycle store; `/epic:plan` emits a validate.js-passing epic file; `/epic:fold` folds with provenance + conflict detection.
- [ ] Every epic/sprint plan carries the 20 acceptance-criteria categories — proven by a planted omission caught by `/scan:ac-coverage`.
- [ ] Planning principles live in canonical homes with a named enforcer.
- [ ] `node scripts/trackers/validate.js` stays 20/20 + `/scan:full` blocking gates stay green; all § Human Approval Points resolved.

## Related definitions
- Mode — see ../../TRACKER.md (live enterable: solo/adhoc/oneshot/sprint).
- Dispatch, Wiring, Verification, Evidence, Completion — see ../../TRACKER.md.

## Related sprints
- **S-LC-01 — Mode-Lifecycle Registry keystone (Wave 0)** — Completed 2026-06-09 @76da472 (gauntlet-clean). Sprint dir: `.claude/project/sprint/sprints/S-LC-01/`.
- **S-LC-02 — Lifecycle-Event Registry + logger + coverage enforcer (Wave 0)** — Completed 2026-06-09 @d23e212 (gauntlet-clean, both lanes PASS on gemini). Sprint dir: `.claude/project/sprint/sprints/S-LC-02/`. **Wave 0 is COMPLETE with both S-LC-01 + S-LC-02 done.**
- **S-LC-03 — Mode-switch preflight guard (REAL guard #1, Wave 1)** — Completed 2026-06-10 @june-9 (gauntlet-clean, full 3-lane incl. security). Sprint dir: `.claude/project/sprint/sprints/S-LC-03/`. Wired REPORT-ONLY.
- **S-LC-04 — mode:init:gate generalization (REAL guard #2, Wave 1)** — Completed 2026-06-10 @june-9 (gauntlet-clean; 1 fix-cycle: dead-counter false-green fixed + re-reviewed PASS). Sprint dir: `.claude/project/sprint/sprints/S-LC-04/`. Wired REPORT-ONLY.
- **S-LC-05 — Persistent-Team Lifecycle Manager + SessionEnd backstop (Wave 1)** — Completed 2026-06-10 @june-9 (gauntlet-clean; 1 fix-cycle: missing verify-terminated added + re-reviewed PASS). Sprint dir: `.claude/project/sprint/sprints/S-LC-05/`. Wired REPORT-ONLY. **Wave 1 is COMPLETE with S-LC-03 + S-LC-04 + S-LC-05 done (all report-only).**
- **S-LC-06 — Dispatch matrix (mode_profiles) + parallelism axis + coverage-gate live wiring (Wave 2)** — Completed 2026-06-10 @june-9 `975ed5c` (gauntlet-clean, full 3-lane all PASS on gemini, α-verified). Mode-scoped permission profile (narrow-only, class∩mode) + `alpha_only_shapes` honesty (ED-041); parallelism findings (advisory); `coverage-gate.js evaluate()` wired into `/scan:full` via `coverage-gate-scan.js`. Sprint dir: `.claude/project/sprint/sprints/S-LC-06/`. Report-only.
- **S-LC-07 — Turbo hardening: spend ceiling + permission profile + classifier-preflight (Wave 2)** — Completed 2026-06-10 @june-9 `95544ac` (gauntlet-clean; 1 fix-cycle: qa-lane found 2 spend-ledger spoof blockers — prototype-key→NaN bypass + negative-byte under-report — fixed @f4eb8d7 with `Object.hasOwn` guard + `clampBytes`, re-reviewed qa+security PASS, α-verified). `resolveCeiling` source-vs-instance ($100 framework default, never hardcodes $500); push-to-main confirm-pin (P-061); `/scan:turbo-spend` consumer; ADR-0011. Sprint dir: `.claude/project/sprint/sprints/S-LC-07/`. Report-only. **Wave 2 is COMPLETE with S-LC-06 + S-LC-07 done.**
- **S-LC-08 — Planning-context binding: _planning lifecycle store + planning-principles enforcer (Wave 3)** — Completed 2026-06-10 @june-9 `1a6d638` (gauntlet-clean, full 3-lane all PASS on gemini, α-verified). Scaffolds `_planning/{epics,sprints,playbooks,decisions,research,archive}/` as a tracker-linked lifecycle store (ship-boundary preserved — all inherit `_planning/`'s walk-skip + MUST_NOT_SHIP via prefix) + `/scan:planning-principles` report-only enforcer + references `_planning/principle.md` from `/sprint:plan`. Sprint dir: `.claude/project/sprint/sprints/S-LC-08/`. Report-only.
- **S-LC-09 — Epic-command suite: design 10, build /epic:plan + /epic:fold (Wave 3)** — Completed 2026-06-10 @june-9 `db42f9a` (gauntlet-clean, backend+qa PASS on gemini, α-verified). 10 `/epic:*` specs (8 design-only + `plan`/`fold` full); `/epic:plan` emits a section-complete epic file + `_planning/epics/` companion with round-trip linkage; `/epic:fold` 14-class taxonomy + provenance + conflict-flag. Sprint dir: `.claude/project/sprint/sprints/S-LC-09/`. **Wave 3 is COMPLETE with S-LC-08 + S-LC-09 done.**
- **S-LC-10 — Provider-tier readiness (Wave 4)** — Completed 2026-06-10 @june-9 `4a5e508` (gauntlet-clean, full 3-lane all PASS on gemini incl. β's 3 security flags, α-verified). Value-free, report-only T1/T2/T3 tier engine (`scripts/mc/provider-tier-check.js`) over the existing health stack; preferred-tier config (confirm-gated, `providerTierConfig` paths key, config-driven T3 floor default `max_5x`); self-attestation + opt-in probe, no infer-from-dispatch; report-only into `/warp:health` + `/scan:environment`. Sprint dir: `.claude/project/sprint/sprints/S-LC-10/`.
- **S-LC-11 — Acceptance-criteria 20-category enforcement (Wave 5)** — Completed 2026-06-10 @june-9 `d946f43` (gauntlet-clean; 1 fix-cycle: backend caught a /epic:plan checklist idempotency blocker → fixed @795123c marker-deduped → re-review PASS; α-verified 12/12). Adds the 20 enforcement-criteria categories (§11) as a 2nd AC axis: `ac-categories.js` single-source + `checkCategoryCoverage()` + `--categories` mode (extends check-ac-coverage.js); /epic:plan scaffolds the checklist, /sprint:design requires it. Report-only. Sprint dir: `.claude/project/sprint/sprints/S-LC-11/`.
- **S-LC-12 — Capstone: playbook design + dry-run sim + e2e pilot (Wave 5)** — Completed 2026-06-10 @june-9 (α-led validation + design synthesis). Playbook-suite DESIGN (reference-only per §22 #6; `/playbook:run` deferred) → `_planning/playbooks/SUITE-DESIGN.md`; full dry-run simulation (21 regression suites + 8 live enforcers + 5 e2e mode-lifecycle pilot probes, **0 defects**) → `_reports/E-LIFECYCLE-001-capstone-validation.md`. **Wave 5 COMPLETE; epic CODE-COMPLETE (12/12).** Sprint dir: `.claude/project/sprint/sprints/S-LC-12/`.

## Dependencies
- E-SYSTEM-ORG-001 (dispatch-shape north star, ~99%) — prerequisite; this epic builds on `dispatch-contract.json`, `dispatch-shape.js`, `safe-spawn.js`, `coverage-gate.js`. State: near-complete on `main`.
- E-DISPATCH-INTEGRITY-001 (coverage-honesty) — sibling; its run-ledger precondition work pairs with Wave 2's coverage-gate wiring. State: Active.
- Operator decisions on the § Human Approval Points (turbo push/spend default; provider funding/subscription detection method; mode-behavior backward-compat) before the affected waves flip to blocking. **RESOLVED 2026-06-09 (operator) — see Change log: all 6 §22 points ruled (turbo spend = $100 framework default, push per-action; provider = self-attestation + opt-in probe; mode-behavior backward-compat DEFERRED to the Wave-1 gate-flip).**

## Blockers
- None currently recorded (Active; Wave-0 keystone landed 2026-06-09 @76da472. The Wave-1 gate-flips remain operator-gated per the § Human Approval Points resolutions, but no blocker prevents S-LC-02 or the Wave-1 build work from starting).

## Risks
- Fail-closed bug bricks mode entry/dispatch — mitigate with the global fail-OPEN posture + kill-switch env/markers + bootstrap allow-list + canary report-only + the dry-run simulation plan.
- Mechanical teardown kills the wrong/another-project team — mitigate with a project-slug scope on every kill + verify-terminated + a planted wrong-project fixture + a `--keep-teams` default for one release.
- Registry generalization regresses the working sprint gate — mitigate by golden-snapshotting the current `team-guard` behavior + keeping `team-guard-gate.test.js` green + a per-mode report→block ramp.
- KNOWN DRIFT to reconcile in S-LC-01: E-SYSTEM-ORG-001 prose records S-12c as "ships DEFAULT-OFF," but the shipped code, `team-guard-gate.test.js`, the code comment, and the live `.team-gate-hard` marker all indicate DEFAULT-ON (read-only-verified 2026-06-08). The reconciliation is a logged Change Log entry, not a silent edit.

## Decisions
- 2026-06-08 — One epic (not split) — the spine (Mode-Lifecycle Registry → lifecycle-event registry → init-gate → team manager) is shared across all areas; splitting fragments the source of truth. Provider readiness (Wave 4) is the one cleanly-separable workstream and may fast-follow as E-PROVIDER-TIER-001 if it grows.
- 2026-06-08 — "25 hook events" reframed to 2 real PreToolUse guards + ~23 virtual registry-driven events logged to `paths.eventsFile` — the harness hook-event set is closed at 8 (read-only-verified).
- 2026-06-08 — Extend, don't reinvent: `dispatch-contract.json` IS the dispatch permission matrix; the provider-health stack already exists. The epic layers onto both.
- 2026-06-08 — Turbo default = highest autonomy (operator decision, β-escalated Class C): `/session:turbo` auto-grants the max autonomy short of the never-allowed hard ceilings. Deliberately overrides "Push = Ask first" for turbo sessions. **[DELIVERY CORRECTED 2026-06-08 — see the GPT-5.5 review decision below: push-to-main is NOT auto-grantable; the harness classifier sits above `permissions.allow` (which already grants `git push *`). Turbo auto-grants spend + commit + branch; push/merge-to-main stays per-action.]** (S-LC-07.)
- 2026-06-08 — β consult (5 calls, `EVT-lifecycle-epic-decomp-2026-06-08`, logged to `paths.betaEvents`): one epic [DECIDE 0.87] (provider readiness = candidate split evaluated at Wave-4 kickoff, not pre-committed); sequencing sound [DECIDE 0.90] (W1 slips with W0, never around it); epic suite design-10-build-2 [DECIDE 0.85] (other 8 explicitly gated as fast-follow); provider detection = self-attestation + opt-in probe, reject infer-from-dispatch [DECIDE 0.84]; turbo [ESCALATE] → operator decision above.
- 2026-06-08 — Post-authoring GPT-5.5 cross-provider review (cabinet, `ok:true`, model gpt-5.5) + α feasibility check CONVERGED → verdict NEEDS-REWORK on OVERCLAIMS (not the foundation); β-DECIDE 0.91 to fold doc-only honesty corrections. The 4 corrections (folded into the plan §1A/§8.3/§8.4/§8.8/§20/§22/§25): (1) turbo auto-push proven infeasible — `git push *` is already in `permissions.allow` yet the harness classifier still blocks push-to-main → push stays per-action; (2) mode-switch enforcement moves into `mode-set.js` as the single lifecycle-transaction writer, PreToolUse guard = BACKSTOP-ONLY; (3) team teardown downgraded mechanical→best-effort+reconcile; (4) blast-radius = fail-OPEN on parse, fail-CLOSED only on explicit bad states, report-only ramp. Evidence: `runtime/agent-system-plan/lifecycle-review/gpt55-review.out`. Foundation intact.

## Open questions
- Turbo auto-push/merge + spend default — RESOLVED 2026-06-08 (operator: "highest autonomy possible") — see § Decisions. Residual sub-call: the exact framework-default spend-ceiling value ($100 baseline recommended). **RESOLVED 2026-06-09 (operator): $100 framework-default ceiling (raisable per session); push/merge-to-main stays per-action. Encoded as DP-gap #39(a) in `paths.decisionPolicy`.**
- Provider funding/subscription detection method (self-attestation vs gated billing-API probe vs infer-from-dispatch) — likely not read-only-detectable — owner: operator. **RESOLVED 2026-06-09 (operator): self-attestation default + optional opt-in billing-API probe; REJECT infer-from-dispatch.**
- T3 tier subscription floor (`Max 5x` vs a lower hobbyist Claude sub — source's own open Q) — owner: operator. **DEFERRED 2026-06-09 (operator) to Wave 4 design.**
- Should provider readiness become its own epic (E-PROVIDER-TIER-001) now or stay Wave 4 — owner: President + operator.

## Session log
### 2026-06-08 — Session (adhoc)
- Agent(s): Alex α + β consult + 6 parallel Phase-0 investigators · Mode: adhoc
- Work performed: authored the full epic plan (`_planning/mc-lifecycle-plan.md`, 26 sections) + cleaned planning principles (`_planning/principle.md`) from the source prompt `_planning/ingest/mc-lifecycle.md`; ran a 6-surface read-only Phase-0 investigation (evidence under `runtime/agent-system-plan/lifecycle-phase0/`); created this epic tracker file; registered the epic in ROADMAP.md § Planned epics + the TRACKER.md header pointer.
- Files changed: `_planning/mc-lifecycle-plan.md`, `_planning/principle.md`, this file, `ROADMAP.md`, `TRACKER.md` · Paths changed: created `trackers/epics/E-LIFECYCLE-001-mode-lifecycle-enforcement.md` · Wirings changed: ROADMAP epic registry + TRACKER header pointer.
- Decisions: see § Decisions (one epic; reframe 25→2+23; extend not reinvent). · Issues discovered: the DEFAULT-ON drift (see § Risks); `lib/mode.js#getMode()` has no sprint case; turbo default-scope drift; authorization-gate ordering drift.
- Definitions added/changed: None.
- State change: (new) → Planned · Completion change: — → 0%
- Verification performed: 6 read-only Phase-0 investigations (≥2-method absence checks) · Validation run: `node scripts/trackers/validate.js` (post-wiring) · Validation result: see Evidence log.
- Next action: see § Current next action.
- Evidence/references: `runtime/agent-system-plan/lifecycle-phase0/0{1..6}-*.md`; `_planning/mc-lifecycle-plan.md`.

## Change log
### 2026-06-10 — Epic retro synthesis (RI-001-deferred close-out)
- Changed: wrote the epic retrospective → `_reports/E-LIFECYCLE-001-epic-retro.md` (the RI-001-deferred retro half of the close-out) and reconciled the Current-state/percent residual list to mark the retro DONE while keeping the epic Active ~92%.
- Reason: the `/sprint:retrospective` skill is per-sprint (transitions ONE `closed` sprint → `retrospected`) and does not span an epic, so the epic-level retro across S-LC-01..12 was synthesized manually (`/learn:deep`-style) from the epic file + the capstone validation report + the per-sprint records — exactly the path RI-001's "retro deferred to epic close" anticipated.
- Scope/honesty: docs-only, no code touched. The retro does NOT set Completed/100% — it explicitly leaves the two operator-gated items open: (1) §22 #4 sign-off for the report-only → blocking flip; (2) the GPT 2nd-pass on the gemini-only security lanes (§H). It also records that capstone residual #4 (the broken `check-ac-coverage` test) was fixed in the adjacent Lane A sprint SP-20260610-001 / T-20260610-295 @7d16332.

### 2026-06-08 — Session (adhoc)
- Changed: created E-LIFECYCLE-001 (epic file + ROADMAP § Planned epics entry + TRACKER header pointer) from the authored plan.
- Reason: the operator directed turning `_planning/ingest/mc-lifecycle.md` into an epic + sprint plan and representing it in the roadmap + tracker.
- Affected: ROADMAP.md (epic registry), TRACKER.md (header pointer), trackers/epics/ (new file), _planning/ (plan + principles).
- Previous state: not tracked (the work was named only in prose as the TRACKER header's "NEXT epic").
- New state: Planned, 0%, plan authored + Phase-0 grounded.

### 2026-06-09 — Session (adhoc)
- Changed: all 6 § Human Approval Points (plan §22) RESOLVED by the operator, and the 5 staged β recommendations PROMOTED into the judgment model. Specifically — #1 epic ID+title APPROVED; #2 turbo/spend APPROVED ($100 framework-default ceiling, raisable per session; auto-grants spend+commit+branch within the never-allowed hard ceilings; push/merge-to-main STAYS per-action because the harness classifier sits above `permissions.allow`); #3 provider detection APPROVED self-attestation default + optional opt-in billing-API probe, REJECT infer-from-dispatch (T3 tier-value DEFERRED to Wave 4); #4 backward-incompatible mode behavior DEFERRED to the Wave-1 gate-flip; #5 `epic:` command names APPROVED all 10, build `/epic:plan`+`/epic:fold` first; #6 Playbook direction APPROVED reference-procedures, design `/playbook:run` later. β recs PROMOTED: P-060 (review-discipline), P-061 (honesty-over-optimism), P-062 (autonomy-ceiling-resolves-upward), P-063 (procedure-integrity) → judgment-model.md; G-20 (pre-build review-gating) → judgment-model.md Reasoning/Heuristic; DP-gap #39(a) (turbo operator-declared override + autonomy-ESCALATE-resolves-upward) → decision-policy.md.
- Reason: the operator ruled on the §22 approval points and directed PROMOTE on all five staged β recommendations from the 2026-06-08→09 mining cycle.
- Affected: `_planning/mc-lifecycle-plan.md` (§22 annotated with resolutions), `.claude/agents/president/_system/beta/judgement-model.md` (P-060..P-063 + G-20 + changelog rows), `.claude/agents/president/_system/policy/decision-policy.md` (DP-gap #39(a) § Autonomy-ceiling resolution), `.claude/agents/president/_system/beta/judgement-model-recommendations.md` (INTEGRATION STATUS marked operator-ruled), this file.
- Previous state: Planned, 0%, §22 approval points open (3 RESOLVED-2026-06-08 + 3 open); 5 β recs LEFT STAGED for operator ruling.
- New state: Planned, 0%, all §22 approval points resolved (4 APPROVED, #3 partial-defer of the T3 sub-call, #4 DEFERRED to Wave-1 gate-flip); 5 β recs promoted. Execution remains approval-gated (no code work started).

### 2026-06-09 — S-LC-01 (Wave-0 keystone) LANDED — epic Activated
- Changed: Wave-0 keystone sprint **S-LC-01 — Mode-Lifecycle Registry** executed and landed on branch `june-9` @ `76da472`, gauntlet-clean. Deliverables: new `.claude/agents/_org/mode-lifecycle.json` (per-mode SoT registry) + `scripts/hooks/lib/mode-lifecycle.js` (shape-validated reader, fail-open) + `scripts/checks/mode-lifecycle-registry.js` (fail-closed drift validator, wired report-only into `/scan:full`); `lib/mode.js#getMode()` sprint-case fix + new `isSprint()`; de-dup of `session-start.js` TEAM_MODES + `team-guard.js` FACE_TYPES to READ the registry (golden suites unchanged — team-guard-gate 13/13, team-guard-sprint 8/8); `mode.json` registered as the `modeMarker` paths key; the S-12c DEFAULT-ON drift reconciled in `TRACKER.md` + E-SYSTEM-ORG-001. Tests: mode-getmode 3/3, mode-lifecycle-registry 18/18. Gauntlet: backend-reviewer + qa-reviewer both PASS (gemini failover after a codex transient reap); 6 findings across 3 fix-cycles all resolved.
- Reason: operator-confirmed execution session for the Wave-0 keystone (the registry every downstream Wave-1 gate reads — it had to land first as the sole SoT).
- Affected: `.claude/agents/_org/mode-lifecycle.json` (new), `scripts/hooks/lib/mode-lifecycle.js` (new), `scripts/checks/mode-lifecycle-registry.js` (new), `lib/mode.js`, `scripts/hooks/session-start.js`, `scripts/hooks/team-guard.js`, the paths registry (`modeMarker` key); this file (Current state, Percent completion, Current next action), `.claude/project/sprint/sprints/S-LC-01/{current,progress}.yaml` (status→done), `ROADMAP.md` (epic Activated + S-LC-01 ledger row), `TRACKER.md` (Active reconciliation).
- Previous state: Planned, 0%, execution approval-gated (no code work started).
- New state: **Active, ~10%** — 1 of 12 planned sprints landed (the Wave-0 keystone); Wave-0 S-LC-02 + the Wave-1 gates are the open next steps.

### 2026-06-09 — S-LC-02 (Lifecycle-Event Registry) LANDED — Wave 0 COMPLETE
- Changed: the other Wave-0 sprint **S-LC-02 — Lifecycle-Event Registry + logger + coverage enforcer** executed and landed on branch `june-9` @ `d23e212`, gauntlet-clean (both lanes PASS on gemini). Deliverables (additive, report-only): new `.claude/agents/_org/mode-lifecycle-hooks.json` (23 virtual events, schema `mc/mode-lifecycle-hooks/v0.1`, `harness_fires:false`) + `scripts/hooks/lib/lifecycle-events.js` (labels-only, fail-open logger) + `scripts/checks/mode-lifecycle-hooks-coverage.js` (report-only coverage enforcer — exit 0 clean/gap, exit 2 parse, P-053 loud-fail; wired report-only into `/scan:full`); `tests/regression/S-LC-02/*` (3 suites, 27 cases incl. a default-registry-resolution regression); the `modeLifecycleHooks` paths key registered. A post-merge path-doubling bug in the coverage enforcer was caught by α's canonical verify, fixed + regression-tested. With S-LC-01, this **COMPLETES Wave 0** (both foundation registries now exist — the Wave-1 mechanical gates that read them are unblocked).
- Reason: operator-confirmed execution session for the second Wave-0 sprint (the lifecycle-event registry the epic plan reframed "25 hook events" into — 2 real PreToolUse guards land in Wave 1; the ~23 virtual events log to `paths.eventsFile` from here).
- Affected: `.claude/agents/_org/mode-lifecycle-hooks.json` (new), `scripts/hooks/lib/lifecycle-events.js` (new), `scripts/checks/mode-lifecycle-hooks-coverage.js` (new), `tests/regression/S-LC-02/*` (new), the paths registry (`modeLifecycleHooks` key), `/scan:full` (report-only wiring); this file (Percent completion, Related sprints, Current next action), `.claude/project/sprint/sprints/S-LC-02/{current,progress}.yaml` (status→done), `ROADMAP.md` (S-LC-02 ledger row + Active-epics entry), `TRACKER.md` (Active reconciliation).
- Previous state: Active, ~10% — 1 of 12 sprints landed (Wave-0 keystone S-LC-01); Wave 0 incomplete.
- New state: **Active, ~17%** — 2 of 12 planned sprints landed; **Wave 0 COMPLETE** (S-LC-01 + S-LC-02 both done); the Wave-1 mechanical gates (S-LC-03/04/05) are now unblocked and the open next steps.

### 2026-06-10 — Wave 1 (3 mechanical-gate sprints) LANDED — Wave 1 COMPLETE (report-only)
- Changed: the three Wave-1 mechanical-gate sprints **S-LC-03 (mode-switch preflight guard)** + **S-LC-04 (mode:init:gate generalization)** + **S-LC-05 (persistent-team lifecycle manager)** executed and merged on branch `june-9` 2026-06-10, all gauntlet-clean (full 3-lane incl. security; 2 fix-cycles total — S-LC-04 dead-counter false-green + S-LC-05 missing verify-terminated, both fixed + re-reviewed PASS). Deliverables: `scripts/hooks/mode-lifecycle-guard.js` (REAL PreToolUse guard #1, /mode:* preflight, emits mode:switch:requested→mode:switch:preflight) + `scripts/hooks/session-end-team-teardown.js` (SessionEnd best-effort project-slug-scoped teardown backstop) + the generalized init-gate + lifecycle-manager code, all wired **REPORT-ONLY** — the flip-to-blocking needs §22 #4 operator sign-off (NOT yet done). Integration-finish (this entry): the 2 deferred hook entries wired REPORT-ONLY into `_mc/settings/defaults.json` (→ recompiled `.claude/settings.json`, compile --check OK) + registered in `framework/hooks.registry.json` (both fail-open); manifests + maps regenerated (manifest validate OK; maps hooks/enforcements 93→95); tracker reconciled. NOTE: a GPT 2nd-pass on the Wave-1 security lanes is owed (codex was in sustained reap → gemini-only security verdicts; conscious acceptance, §H) before the blocking flip.
- Reason: operator-confirmed execution of the Wave-1 mechanical gates (the gates that READ the Wave-0 registries), plus the mechanical integration-finish for the two builder-deferred hook wirings (deferred to avoid a settings.json parallel-build collision).
- Affected: `scripts/hooks/mode-lifecycle-guard.js` (new), `scripts/hooks/session-end-team-teardown.js` (new), the S-LC-04 init-gate + S-LC-05 lifecycle-manager code/tests, `_mc/settings/defaults.json` + `.claude/settings.json` (2 report-only hook wirings), `framework/hooks.registry.json` (2 entries), the regenerated maps + manifests; this file (Current state, Percent completion, Related sprints, Current next action), `.claude/project/sprint/sprints/S-LC-0{3,4,5}/{current,progress}.yaml` (status→done), `ROADMAP.md` (S-LC-03/04/05 ledger rows + completion bump), `TRACKER.md` (Active reconciliation).
- Previous state: Active, ~17% — 2 of 12 sprints landed; Wave 0 complete, Wave 1 open.
- New state: **Active, ~42%** — 5 of 12 planned sprints landed; **Wave 0 + Wave 1 COMPLETE** (S-LC-01..05, Wave-1 all report-only); Wave 2 (S-LC-06 dispatch parallelism + coverage-gate wiring; S-LC-07 turbo hardening) is the open next step.

### 2026-06-10 — Wave 2 (dispatch + turbo) LANDED — Wave 2 COMPLETE (report-only)
- Changed: the two Wave-2 sprints **S-LC-06 (dispatch mode_profiles + parallelism axis + coverage-gate live wiring)** + **S-LC-07 (turbo hardening — spend ceiling + permission profile + classifier-preflight)** executed and merged on `june-9` 2026-06-10 (`975ed5c`, `95544ac`), both gauntlet-clean (full 3-lane on gemini, α-verified by re-running each lane and reading the real VERDICT — the records carry `ok:true` liveness only). S-LC-06: all 3 lanes PASS first pass; FINDING-1 fold added `alpha_only_shapes:["in-process-agent"]` to sprint mode + corrected the false "ε dispatches roster via Agent tool" premise (a teammate-ε cannot call the Agent tool — ED-041). S-LC-07: 1 fix-cycle — the qa lane (integrity scope) caught 2 spend-ledger spoof BLOCKERS that backend+security lanes missed (prototype-key model `"constructor"` → `NaN` total → ceiling bypass; negative `prompt_bytes` → under-report), fixed @`f4eb8d7` (`Object.hasOwn(PRICE_TABLE,model)` guard + `clampBytes` non-negative-finite clamp + 5 regression assertions), re-reviewed qa+security PASS. Cross-provider-catches-false-green pattern confirmed again. ADR-0011 records the source-vs-instance ceiling + push-to-main confirm-pin (P-061). All wired **REPORT-ONLY**.
- Reason: operator "do it all / finish the work" mandate — Wave 2 of E-LIFECYCLE-001 (the dispatch + turbo workstreams).
- Affected: `.claude/agents/_org/dispatch-contract.json` (mode_profiles + alpha_only_shapes), `scripts/dispatch/dispatch-contract.js` (validator), `scripts/dispatch/dispatch-shape.js` (parallelism), `scripts/checks/coverage-gate-scan.js` (new), `.claude/commands/scan/full.md` (coverage-gate wiring), `scripts/turbo/{spend-ledger,permission-profile,classifier-preflight,apply}.js`, `scripts/checks/turbo-spend.js` (new), `.claude/commands/scan/turbo-spend.md` (new), `tests/regression/S-LC-0{6,7}/*` (new suites), `CLAUDE.md` + `.claude/commands/mode/sprint.md` (ED-041 honesty), ADR-0011 (new); this file, `active-sprints.yaml` (S-LC-06/07 → closed), `ROADMAP.md`, the regenerated manifests + maps, `TRACKER.md`.
- Previous state: Active, ~42% — 5 of 12 sprints landed; Wave 0+1 complete, Wave 2 open.
- New state: **Active, ~58%** — 7 of 12 planned sprints landed; **Wave 0 + Wave 1 + Wave 2 COMPLETE** (S-LC-01..07, all report-only); Wave 3 (S-LC-08 planning-context binding + S-LC-09 epic-command suite) is the open next step.

### 2026-06-10 — Wave 3 (planning + epic-suite) LANDED — Wave 3 COMPLETE (report-only)
- Changed: the two Wave-3 sprints **S-LC-08 (_planning lifecycle store + planning-principles enforcer)** + **S-LC-09 (epic-command suite, design-10-build-2)** executed and merged on `june-9` 2026-06-10 (`1a6d638`, `db42f9a`), both gauntlet-clean (α-verified by re-running each lane for the real VERDICT). S-LC-08: 3-lane (backend+security+qa) all PASS — the `_planning/` ship-boundary (walk-skip + MUST_NOT_SHIP) is preserved (new subdirs inherit via prefix); `/scan:planning-principles` is report-only + fail-open. S-LC-09: 2-lane (backend+qa) PASS — design-10-build-2 holds (8 design-only specs + `/epic:plan`+`/epic:fold` built), `/epic:plan` emits a section-complete epic file, `/epic:fold` uses the 14-class taxonomy with provenance + conflict-flag. β's Wave-3 boundary consult (all 4 DECIDE) confirmed parallel-build, low ship-boundary risk, scope discipline, and the gauntlet differentiation. A Wave-1-tail manifest-honesty gap surfaced + fixed: S-LC-05's `scripts/teams/lifecycle.js` (backing the shipped SessionEnd teardown hook) was never in the framework-manifest ASSET_DIRS → ship-coverage FAIL; added `scripts/teams` + `scripts/epic` to ASSET_DIRS, ship-coverage now GREEN (1454 paths, 0 gaps).
- Reason: operator "do it all / finish the work" mandate — Wave 3 of E-LIFECYCLE-001 (the durable planning system + epic suite).
- Affected: `_planning/{epics,sprints,playbooks,decisions,research,archive}/` (new scaffold + READMEs) + `_planning/README.md`, `scripts/checks/planning-principles.js` (new), `.claude/commands/scan/planning-principles.md` (new) + `/scan:full` wiring, `.claude/commands/sprint/plan.md` (principle pointer), `.claude/commands/epic/*.md` (10 new), `scripts/epic/{plan,fold,lib}.js` (new), `tests/regression/S-LC-0{8,9}/*` (new), `scripts/generate-framework-manifest.js` (ASSET_DIRS += scripts/teams, scripts/epic), the regenerated manifests + maps; this file, `active-sprints.yaml` (S-LC-08/09 → closed), `ROADMAP.md`, `TRACKER.md`.
- Previous state: Active, ~58% — 7 of 12 sprints landed; Wave 0+1+2 complete, Wave 3 open.
- New state: **Active, ~75%** — 9 of 12 planned sprints landed; **Wave 0 + Wave 1 + Wave 2 + Wave 3 COMPLETE** (S-LC-01..09, all report-only); Wave 4 (S-LC-10 provider tier readiness) is the open next step.

### 2026-06-10 — Wave 4 + Wave 5 LANDED — epic CODE-COMPLETE (12/12, report-only)
- Changed: the final three sprints **S-LC-10 (provider-tier readiness)** @4a5e508 + **S-LC-11 (AC 20-category enforcement)** @d946f43 + **S-LC-12 (capstone validation, α-led)** executed and merged/completed on `june-9` 2026-06-10. S-LC-10 + S-LC-11 built in PARALLEL (operator "parallel where safe"; β Wave-4 + Wave-5 consults both all-DECIDE). S-LC-10 3-lane gauntlet all PASS (β's 3 security flags — value-free, .env-untouched, read-only health hook-in — confirmed by the security lane). S-LC-11 1 fix-cycle (idempotency blocker → fixed @795123c → re-review PASS). S-LC-12 (capstone): playbook-suite DESIGN (reference-only) + the full dry-run simulation — 21 regression suites + 8 live enforcers + 5 e2e mode-lifecycle pilot probes, **0 defects** (the guard resolved the required team from the registry on a live /mode:sprint payload + emitted a report-only preflight; fail-open held). A Wave-1-tail manifest-honesty gap (S-LC-05 `lifecycle.js` + `scripts/epic/` unshipped) was caught by ship-coverage + fixed (ASSET_DIRS; ship-coverage GREEN, 1454 paths/0 gaps).
- Reason: operator "do it all / finish the work / no lazy-mode" mandate — drive E-LIFECYCLE-001 to code-completion.
- Affected: `scripts/mc/provider-tier-check.js` + `lib/provider-tier-config.js` (new), `scripts/sprint/ac-categories.js` + `check-ac-coverage.js` (extended), `scripts/epic/plan.js` (AC scaffold), `_planning/playbooks/SUITE-DESIGN.md` + `_reports/E-LIFECYCLE-001-capstone-validation.md` (new), `scripts/generate-framework-manifest.js` (ASSET_DIRS += scripts/teams, scripts/epic), wirings into `/warp:health` + `/scan:environment` + `/sprint:design` + `/scan:ac-coverage`, the regenerated manifests + maps; this file, `active-sprints.yaml` (S-LC-10/11/12 → closed), `ROADMAP.md`, `TRACKER.md`.
- Previous state: Active, ~75% — 9 of 12 sprints landed; Wave 0–3 complete.
- New state: **Active, ~92% — CODE-COMPLETE (12/12 sprints, all report-only).** Remaining ~8% is operator-gated/owed, NOT code: (1) §22 #4 operator sign-off for the report-only→blocking flip; (2) the GPT 2nd-pass on the gemini-only security lanes (§H); (3) the epic retro. Epic stays Active until the flip decision.

## Evidence log
### 2026-06-08 — Epic plan authored + Phase-0 grounded
- Evidence type: File changed.
- Detail/location: `_planning/mc-lifecycle-plan.md` (26-section plan); `_planning/principle.md`; `runtime/agent-system-plan/lifecycle-phase0/0{1..6}-*.md` (6 grounded investigations).
- Verified by: Alex α · Supports: DoD scoping + § Scope (the plan exists and is grounded in real repo state).

## Verification log
| Item | Should exist? | State | Where / wired where | Proof (cmd/inspection) | Checked | By |
| --- | --- | --- | --- | --- | --- | --- |
| `_planning/mc-lifecycle-plan.md` | Yes | Verified Exists | `_planning/` | Write confirmed 2026-06-08 | 2026-06-08 | Alex α |
| `.claude/agents/_org/mode-lifecycle.json` (registry keystone) | Yes (to be built — S-LC-01) | Missing But Required | `.claude/agents/_org/` | grep two ways → absent | 2026-06-08 | Phase-0 investigators |
| `mode:init:gate` (named) | Yes (to be built — generalize team-guard) | Missing But Required | `scripts/hooks/team-guard.js` (template exists) | grep → only planning docs | 2026-06-08 | Phase-0 investigators |
| `dispatch-contract.json` (the matrix) | Yes | Verified Exists | `.claude/agents/_org/` | read 2026-06-08 | 2026-06-08 | Phase-0 investigators |
| Provider-health stack | Yes | Verified Exists | `scripts/hooks/lib/provider-health.js` + 4 more | read 2026-06-08 | 2026-06-08 | Phase-0 investigators |
| Harness hook events = closed set of 8 | Yes | Verified Exists | `.claude/settings.json` | grep -o two ways | 2026-06-08 | Phase-0 investigators |

## Current next action
**ALL 5 WAVES COMPLETE — epic CODE-COMPLETE (12/12 sprints), LANDED ON `main` (`5d4a824`, pushed 2026-06-10).** Wave 0 (S-LC-01+02) · Wave 1 (S-LC-03+04+05) · Wave 2 (S-LC-06+07) · Wave 3 (S-LC-08+09) · Wave 4 (S-LC-10 @4a5e508) · Wave 5 (S-LC-11 @d946f43 + S-LC-12 capstone). Capstone dry-run sim GREEN, 0 defects (`_reports/E-LIFECYCLE-001-capstone-validation.md`). The push+merge-to-main tap is DONE.

**OPERATOR AUTHORIZATIONS GRANTED 2026-06-10 (verbatim: "I give you the sign-off, the authorization for GPT 2nd-pass, and then ability to do the epic, and the cleanup").** These are recorded operator approvals (not a mere handoff "continue"). Resolves §22 #4 → **APPROVED** (the report-only→blocking flip is signed off). The NEXT SESSION executes, in order (confirm a one-line "go" at session start per the standing-authorization nuance, then proceed — no re-deliberation needed, the authorization is recorded):
1. **GPT 2nd-pass** on the gemini-only security lanes (codex must be back from reap; §H) — re-review the security surfaces (mode-lifecycle-guard, team-guard init-gate, dispatch mode_profiles, turbo spend-ledger, provider-tier value-free, planning-principles, ac-coverage). Fix any new findings.
2. **Report-only → blocking flip (§22 #4, HIGH-risk):** flip the gates from report-only to blocking INCREMENTALLY, keeping the kill-switch (`WARPOS_DISABLE_TEAM_GATE` / `.team-gate-off`) + fail-open posture; dry-run sim per mode first (§17); a planted "switch with live old team" must block. Do NOT flip all gates at once — ramp per the §21 rollback plan.
3. **Epic retro** (`/sprint:retrospective` or `/learn:deep` → `/learn:integrate` → reconcile) → then set this epic **State: Completed, 100%** + fill the Completion record.
4. **Cleanup:** ✅ DONE 2026-06-10 via sprint SP-20260610-001 ticket T-20260610-295 (Lane A, MC.md-sweep arc) — the broken `tests/regression/SP-20260518-007/check-ac-coverage.test.js` had TWO defects: the stale `check/`→`scan/` path (ENOENT) AND machine-specific absolute paths in the fixture's `current.yaml` (a different-username `C:\Users\...` — made project-relative). Verified 4/4 green; gauntlet-clean (3-lane gemini PASS).
5. **THEN the next roadmap bet (Director-of-Product pick, 2026-06-10):** **E-MC-READINESS-ANALYSIS-001** (the Master Console external-ship gate, analysis-first — findings-only, no changes). Cheap precondition test first: confirm the analysis auditors can resolve a single source of truth per artifact (if the `.system` duplicate-drift would poison the findings, pull E-SYSTEM-ORG-001's audit-only pass forward as analysis sprint #5's input). Runner-up: E-SYSTEM-ORG-001 (audit-only) — but run it AS analysis sprint #5, not standalone.

The full plan remains at `../../_planning/mc-lifecycle-plan.md` § "Proposed Sprint Breakdown".

## Completion record
- Final state: **Not yet Completed — CODE-COMPLETE (12/12 sprints), Active ~92%, report-only.** Held open by operator-gated/owed DoD items (below), not by code.
- Percent completion: ~92% (all 12 sprints landed + capstone-validated 0-defect; the remaining ~8% is the operator-gated blocking flip + GPT 2nd-pass + retro).
- Completion timestamp: n/a (code-complete 2026-06-10; full completion pends the flip decision).
- Definition of done used: the § Definition of Done above.
- Evidence of completion (code): `_reports/E-LIFECYCLE-001-capstone-validation.md` (21 regression suites + 8 live enforcers + 5 e2e pilot probes, 0 defects); 12 merge/close commits on `june-9` (S-LC-01..12).
- Session IDs / dates / agents: 2026-06-09/06-10, α (orchestrator) + ε (gauntlet conductor) + β (boundary consults) + backend/security builders+fixers + cross-provider gemini reviewers.
- Related completed sprints: S-LC-01 … S-LC-12 (all 12).
- Remaining follow-up items (the ~8% — NOT code): (1) §22 #4 operator sign-off → report-only→blocking flip — sign-off RECORDED 2026-06-10, flip EXECUTION still gated on (2); (2) GPT 2nd-pass on the gemini-only security lanes (§H) — BLOCKED as of 2026-06-10: codex/openai `quota_exhausted` (probed twice 2026-06-10, real quota error not a reap; retry when credits restored); (3) epic retro; (4) main-push tap DONE 2026-06-10 (`5d4a824` pushed); (5) cleanup DONE 2026-06-10 (T-20260610-295, see § Current next action item 4).
- Related untracked work: None.
- ../../TRACKER.md updated: Yes (header pointer) · Roadmap reconciled: Yes (§ Planned epics).
