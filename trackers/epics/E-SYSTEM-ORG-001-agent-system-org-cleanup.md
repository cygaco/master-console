<!-- EPIC TRACKER — spec §22. Linked from ../../ROADMAP.md § Epics. Template: ../templates/EPIC_TEMPLATE.md
     States (§19): Planned | Ready | Active | Blocked | Paused | Review Needed | Completed | Cancelled | Superseded -->

# E-SYSTEM-ORG-001 — Agent-System Organization & Canonical Source-of-Truth Cleanup (`.system` files/folders)

- **Epic label and number:** E-SYSTEM-ORG-001
- **Title:** Agent-System Organization & Canonical Source-of-Truth Cleanup (`.system` files/folders)
- **Owner:** President Agent
- **Parent roadmap area:** [../../ROADMAP.md](../../ROADMAP.md) § Epics → Active epics (⭐⭐ TOP, operator-directed 2026-06-07)
- **Goal:** ONE canonical source of truth per agent-system artifact — no duplicate, drifted, orphaned, or mispointed `.system` files/folders. End the recurring "agents keep skipping/misreading the dispatch guide" class and its siblings by removing the structural ambiguity the ADR-0007 org-rewrite left behind.
- **Background:** Operator-directed 2026-06-07 ("add figuring all this out to the top of the roadmap; pull in the tasks on fixing the organization and all the 'system' files and folders"). Forced by a live, diagnosed instance this session: `agent-dispatch-guide.md` exists TWICE and has drifted — `.claude/project/reference/agent-dispatch-guide.md` (197 ln, edited 2026-06-05, post-ADR-0007 roles; the one `paths.agentDispatchGuide` + the session-start banner + CLAUDE.md correctly point to) AND `.claude/agents/.system/guides/agent-dispatch-guide.md` (243 ln, edited 2026-06-02, pre-ADR-0007, orphaned from the pointer but sitting in the intuitive agent-tree home). Both `owner=framework` → both ship downstream. The maintained canonical is in the *unintuitive* place and a stale copy squats in the *intuitive* place, so operator + agents keep pointing at different files; `scan:references` can't catch it (both files exist = not a broken ref; it is content drift). Root pattern: ADR-0007 copied artifacts into the new `.claude/agents/` tree while the maintained canonical stayed at the old location, leaving duplicate/orphan/mispointed canonicals across `.system`. <!-- doc-ref-ignore: historical background; names the pre-consolidation guide location on purpose -->
- **Scope:**
  - S-1 — AUDIT (analysis-first, NO changes): enumerate every duplicate-basename / drifted / orphaned / mispointed canonical across `.claude/agents/**` (especially `.system/**`) and `.claude/project/reference/**`; for each, name the ONE canonical (content-currency + ADR-0007 home) and the disposition (merge→delete, repoint, or remove). Output a frozen findings inventory.
  - S-2 — Dispatch-guide consolidation (the proven first fix): produce ONE canonical `agent-dispatch-guide.md` = current 197-ln content + folded-in still-useful bits from the older copy (the codex-CLI `codex login` auth table + concurrency caps); repoint `paths.agentDispatchGuide` (framework/paths.registry.json) + the session-start banner (`scripts/hooks/session-start.js`) + CLAUDE.md; delete the duplicate; both-layers (specs + scripts/paths) per the rename-cutover-covers-both-layers rule.
  - S-3 — Absorb **E-DISPATCH-INTEGRITY-001 F-4/F-5**: de-dot + restructure the agents folder to one visible convention (no `.system`/`_system`/`.system.md` collision); `cli.js` skip-by-visible-`_` not `startsWith(".")`; both-layers rename.
  - S-4 — Absorb **ED-026 / scan:cutover-completeness**: extend the cutover-completeness gate from raw-deleted-literal greps to duplicate/drift detection.
  - S-5 — Absorb the **"Maintainer canonical scrub orchestration"** Sprint-10 candidate + **E-BOUNDARY-001**: move MC-as-product specs out of canonical root, flip `ROOT_LEAK_PENDING_SCRUB`, the operator-scoped repo move the framework can't self-execute.
  - S-6 — ENFORCER: a duplicate-basename-drift detector (the gap `scan:references` misses — two shipped framework files, same basename, drifted content) wired into `/scan:full` + release gates; so this class is self-detecting going forward.
- **Out of scope:** E-DISPATCH-INTEGRITY-001 F-1/F-2/F-3 (dispatch coverage-honesty / two-world seam / gauntlet-verify correlation — a different slice; stays in that epic). Content rewrites of the guides beyond the merge (that's `/skills:edit`-style authoring, not org).
- **Current state:** Active — EXECUTION session 3 (2026-06-08-pm-3) landed 4 commits on `main` (pushed): the build-chain Claude spawn is now safe-kernel-wired (`05ff3c3`), the N-1 coverage gate is §17.4-strengthened + flipped to BLOCKING (`dc6243d`), dispatch-skill records are §17.4-gate-compliant (`e0af05e`), + maps/manifest convergence (`9509569`). The safety-kernel wire-through (§17.2 step-1) now covers providers.js + dispatch-claude.js + `scripts/dispatch-skill.js` (top-level — its PRODUCTION spawn routes through `safeSpawn.safeSpawnFile`, fails closed if the kernel is missing; the only remaining raw `spawnSync` is the `DISPATCH_SKILL_BIN` test-seam, mirroring dispatch-claude.js; DONE 2026-06-08 `150252b`, reconciled 2026-06-10). No raw production `shell:true` remains in the dispatch layer. **D-3 gauntlet-contract extraction DONE** (`ef3fcf8` + `7ee1488`); **§14 integration-seam extraction DONE 2026-06-08** (`_system/guides/integration-seam-contract.md` + 3 citers repointed + monolith §14 declared extracted); **S-11 Layer-1 handoff safety-net DONE 2026-06-08** (per-sid `handoff-live.js` + freshness/`/clear`-bridge loader + 13/13 fixture); **S-10 deixis advisory DONE 2026-06-08** (report-only); **§13.6 skills:test harness DONE 2026-06-08** (resolve mode + 8/8 fixture). **D-2 `president/_system` de-dot DONE 2026-06-08** (ε-binding-PASS + β-DECIDE; the last both-layers rename — git mv + 19 registry keys + 72-file sweep + cutover needle, gauntlet-green). **2026-06-08 continuation-tail DONE:** S-12c HARD team-readiness gate (`394b696`, DEFAULT-ON [mechanically flipped 2026-06-08; doc reconciled 2026-06-09 — code-authoritative] + 13/13), S-13 to-epic + S-13b doc-ref-integrity enforcer (`ba751b5`, 168→0-broken baseline + 14/14), ED-038 dispatch-attribution honest-limit (`f2fd7fd`), S-11 Layer-2 sentinel-retire (`30937f5`, RI-006 fixed + 7/7). **Remaining = the two OPERATOR-GATED / BOUNDED items only:** §13.7 instrumented heavy A/B measurement (harness + procedure complete + fail-closed-safe — a deliberate $-session, not script-fakeable) and S-5 boundary scrub (operator repo move — no safe in-canonical pre-move advance; see the S-5 DoD).
- **Percent completion:** ~99% — IN-CANONICAL WORK COMPLETE; the open ~1% is the two genuinely operator-gated/bounded items only (§13.7 instrumented measurement + S-5 repo move). 2026-06-08 continuation-tail closed S-12c (`394b696`), S-13/S-13b (`ba751b5`), ED-038 (`f2fd7fd`), S-11 Layer-2 (`30937f5`) — so S-11 + S-12 are now FULLY built. Earlier same-day: §14 + S-11 Layer-1 + S-10 + §13.6 + D-2 de-dot all DONE ( D-2 = the last both-layers rename, ε-binding-PASS + β-DECIDE 0.88; see Change log "2026-06-08 (continuation)"). Earlier @aa60e7e: D-3 gauntlet-contract extraction (`ef3fcf8` — focused authoritative `_system/guides/gauntlet-contract.md` with the parallel-gauntlet/snapshot-diff/circuit-breaker/escalation-ladder/context-scoping contracts, role names modernized to ADR-0007; `7ee1488` — ~7 active citers repointed off the archive monolith [builders, leads, gamma, qa-reviewer, frontmatter-guide, dispatch-claude comment], the §14 integration-seam refs deliberately kept pointed at the archive since D-3 didn't extract §14, monolith banner finalized; cutover/dup-drift/dispatch-resolution green, findAgentSpec does not enumerate the guide/monolith); dispatch-claude.js safe-spawn wire-through (`05ff3c3`, live-proven real claude → ok:true PROBE_OK + structural injection-refused + 9/9 tests); coverage-gate §17.4 record-schema strengthening + FLIP-TO-BLOCKING (`dc6243d` — `ARGV_SCHEMA_VERSION` keystone + `output_digest` proof-of-artifact + `cwd` on all 3 wrappers; evaluate() now requires current-schema + artifact-proof + optional named-artifact on-disk verify + auditable `waiver{reason}`; BLOCKING by default; coverage-gate 14/14 with 10 planted; live BLOCKING gate greens on a real stamped record [exit 0] + blocks a missing role [exit 1]); dispatch-skill §17.4 gate-compliance stamps (`e0af05e`, so a skill record SATISFIES the strengthened gate). Earlier (session 2): S-7 role renames (cabinet/ops-analyst/skeleton-builder, alias-backed, `a071e80`); S-6 + N-2 enforcers flipped to BLOCKING (`b0c6c8e`); safety kernel + auth-resolver WIRED into providers.js (PROBE_OK codex+gemini); S-2 guide consolidation; S-3 top-level de-dot; S-6 duplicate-doc-drift enforcer; N-2 provider-api-policy enforcer; §13 dispatch-skill.js + skill-weight registry; grandchild-kill fixture (safe-spawn 22/22). REMAINING (as of 2026-06-08 continuation): §13.7 full earn-it benchmark MEASUREMENT (the §13.6 `skills-test.js` harness + `dispatch-skill --resolve` mode + 8/8 fixture + §17.5 benchmark scaffold are DONE 2026-06-08; the heavy per-skill A/B measurement is deferred — 0 real skills subprocess_verified, honest); S-11 Layer-2 sentinel-retire/rewire (the Layer-1 per-sid safety net is DONE 2026-06-08 + wired, 13/13 fixture); dispatch-skill safe-spawn wire-through (DONE 2026-06-08 `150252b` — `scripts/dispatch-skill.js` PRODUCTION path routes through `safeSpawnFile`, fail-closed; verified 2026-06-10); `president/_system/` de-dot (D-2, own sprint, DEFERRED — also owns the 2 stale-dotted-path oneshot citers of agent-system.md); S-5 boundary scrub (operator-scoped, DEFERRED).

## Definition of Done
- [x] S-1: frozen duplicate/drift/orphan/mispointed inventory — PLAN.md §1 (the audit; the `agent-dispatch-guide.md` duplicate + the `.system/`/`.system.md` dotted artifacts named with dispositions).
- [x] S-2: ONE `agent-dispatch-guide.md` at `.claude/agents/_system/guides/`; `paths.agentDispatchGuide` + banner + CLAUDE.md all resolve to it; the orphan + the project/reference copy are deleted; gates green; no stale copy ships. (Waves 2a)
- [x] S-3 (top-level): `.claude/agents/.system/` dir + `.system.md` monolith de-dotted → `_system/`; `role-parity` + `cutover-completeness` + `test-dispatch-config.js` green; both-layers repointed. (`president/_system/` DEFERRED — D-2, own sprint.) (Waves 2a/2b)
- [x] S-4: satisfied by S-6 (per D-4, shipped as a SEPARATE `duplicate-doc-drift` scan rather than folded into cutover-completeness; cutover-completeness stays on stale-rename-literals).
- [ ] S-5: MC-as-product specs out of canonical root; `ROOT_LEAK_PENDING_SCRUB` flipped; boundary gate blocks. (**OPERATOR-GATED — verified 2026-06-08 there is NO safe in-canonical pre-move advance.**) The full epic is E-BOUNDARY-001 (scope already turnkey there). Confirmed: `_requirements/` (16 entries incl. the product-slugged `00-canonical/*` + `03-architecture/*`) and `_docs/` (research|briefs|clones|imports) still live at canonical root. (a) Flipping `ROOT_LEAK_PENDING_SCRUB=false` NOW would make `framework-purity.js` emit 23+ `root_leak` findings and BLOCK `/scan:full` (it runs `--strict`) — harmful until the specs relocate. (b) Hardening `framework-purity-guard` to reject `_requirements/00-canonical/*` writes is explicitly the POST-scrub step (SP-20260525-002) and would block legitimate edits to the still-present specs if done early. (c) The relocation target is a NEW private `mc-as-product` sibling repo — forbidden by the autonomy boundary ("MC-only; creating fresh sibling repos needs operator sign-off"). **Next action (operator):** `/portfolio:new --slug mc-as-product` + relocate the specs → then a follow-up session flips the flag + hardens the guard + regens the manifest. Not laziness — a hard autonomy + ordering boundary.
- [x] S-6: duplicate-basename-drift enforcer (`scripts/checks/duplicate-doc-drift.js` + allowlist + 9/9 test, 3 planted) wired into `/scan:full`; a planted drifted duplicate FAILS it. Report-only this release; flip-to-blocking pending. (Wave 1)
- [x] S-7: role renames + re-homes (learner→ops-analyst @ president, stub-scaffold→skeleton-builder @ engineering, consult/advisor→cabinet @ president); both-layers, alias-backed (old names resolve via role-aliases.js); role-parity OK + dispatch-routing-parity exit 0 (guide↔code drift closed) + test-registry-roles 23/23 + normalizeRole smoke + live `runProvider('cabinet')→OK3`. (`a071e80`)
- [x] **S-10 (tracker language hygiene — anti-deixis):** tracker prose is ABSOLUTE + state-chained, never bare session-relative deixis (see "Tracker language convention" below). DoD: (a) the convention is recorded here AND propagated to `TRACKER.md` § How to Use; (b) a `session-relative-language` (deixis-anchor) check in `scripts/trackers/validate.js` flags a banned deictic phrase that lacks an absolute anchor (ISO date or 7–40-hex commit hash) in the same clause, with an allowlist, ramped report-only → blocking; (c) the existing high-read lines (header NEXT ACTION + each epic current-state/percent) are converted to absolute form. Enforcer BUILT 2026-06-08 (report-only ramp stage): the `session-relative-language` advisory in `scripts/trackers/validate.js` flags un-anchored deixis with an allowlist, lives OUTSIDE the 20 binding checks (exit-0 preserved), 70/70 selftest incl. the 3 P5 cases; (a) convention propagated to `TRACKER.md` § How to Use + (c) high-read lines converted = DONE. ED-034 build closed; flip-to-blocking is the ramp tail (tracked). (Added 2026-06-08 @aa60e7e — operator-raised: "'no X this session' confuses the next session.")

- [x] **S-11 (handoff redesign — safety-net live-state):** two-layer handoff — a cheap deterministic per-`Stop` `handoff-live.md` (ground-truth git/asks/tasks; the safety net for tracker-drift + untracked work) + the rich `SessionEnd` narrative — covering new / resume / post-`/clear` / crash. DoD = the 4 load-bearing acceptance criteria in the "Handoff redesign (S-11)" section below. Design 2026-06-08 @9163f77 (`runtime/notes/handoff-redesign-2026-06-08.md`, RI-006). **Layer-1 BUILT 2026-06-08:** per-sid `scripts/hooks/handoff-live.js` (cheap deterministic git ground-truth snapshot; atomic temp+rename; read-only short-timeout fail-open git; per-sid `.session-start-commit-<sid>` baseline immune to session-stop's cleanup; hardened porcelain parser) + the SessionStart load-time freshness comparison + `/clear` bridge + per-source labeling in `session-start.js` + wired BEFORE session-stop on Stop/SessionEnd/StopFailure via `_mc/settings/defaults.json` recompile + 13/13 fixture `scripts/hooks/handoff-live.test.js` + a 0.24s live smoke. All 4 acceptance criteria met. **Layer-2 sentinel-retire BUILT 2026-06-08:** `session-stop.js` retired the once-per-session `.session-handoff-done` freeze sentinel and now gates the rich narrative on the TRIGGER — it runs on `SessionEnd` (close / `/clear` / logout) + manual invocation, and returns early on a plain end-of-turn `Stop`/`StopFailure` (Layer-1 `handoff-live.js` covers those). This is the direct RI-006 fix: the old sentinel let the first `Stop` write+lock so the `SessionEnd` meant to capture FINAL state skipped → frozen handoff + dead `/clear` bridge. The session-scoped `.session-start-commit`/`.compact-summary` cleanup now also correctly fires at close (not after reply #1). The design's watch-period was substituted with a fixture test (additive-first held). Fixture: `scripts/hooks/session-stop-sentinel.test.js` **7/7** — Stop/StopFailure skip; SessionEnd + SessionEnd(clear) + manual write; **NO-FREEZE** (Stop×3 then SessionEnd → handoff appears ONLY at SessionEnd, the exact RI-006 bug); preserve-marker respected.

- [x] **S-12 (system-sourced persistent-team initialization — the once-and-for-all team-init fix):** ROOT FIX, operator-escalated 2026-06-08 after the team-skip recurred a 3rd surface ("you always operate from memory and ignore the entire WarpOS"). Root-caused grounded in the SYSTEM (`.claude/commands/mode/sprint.md` — complete + lesson-encoded; I bypassed it by never invoking `/mode:sprint`) via /reasoning:run + dry-run /fix:deep (RCA): the operating loop lets injected auto-memory SUBSTITUTE for the system at the decision point, and the correct team-init lives only inside the skippable `/mode:<mode>` Step 1.75 — so a `/clear`'d kickoff that names `/mode:sprint` as TEXT never runs it and I improvise the team from a lossy memory summary → wrong/absent roster. **Fix = system-level, at session entry, off persisted `mode.json` (fires REGARDLESS of whether `/mode:sprint` is invoked):** (a) **BUILT** — `scripts/hooks/session-start.js` injects the EXACT mode-specific Step-1.75 calls (the RIGHT faces — α+ε+β sprint / α+β+γ adhoc) as the MANDATORY first action when `mode.json` is an active team-mode and the correct team isn't live; catches the wrong-roster case too (team up but no ε). 5/5 `scripts/hooks/session-start-teaminit.test.js`. (b) **BUILT** — `scripts/hooks/team-guard.js` PreToolUse-on-Agent dispatch-time advisory (ED-035) catching no-team AND team-without-ε; 8/8 `team-guard-sprint.test.js` + regression `test-team-guard` 16/16. (c) **BUILT 2026-06-08 (`394b696`; mechanically flipped to DEFAULT-ON the same day, doc reconciled 2026-06-09 — code-authoritative)** — a HARD PreToolUse readiness gate (`team-guard.js` sprint block) that BLOCKS worker dispatch until the correct team (α+ε+β) is live, with the bootstrap ALLOW-list (faces + research + any `team_name`) airtight; the long-session false-block risk is handled by a sid-keyed heartbeat-liveness check (independent of the 24h config-mtime window), a kill-switch, and fail-open. **DEFAULT-ON** — `hardGate = WARPOS_TEAM_GATE_SOFT !== "1"` (team-guard.js ~L255); the SOFT opt-out (`WARPOS_TEAM_GATE_SOFT=1`) ramps it back to the (b) advisory; the `.team-gate-hard` marker is a legacy belt-and-suspenders force-on, not the enabler. 13/13 `team-guard-gate.test.js` (incl. the "DEFAULT (no flag) … => BLOCK (gate ships ON)" case) + regressions green. **Principle:** memory POINTS to the system; the system EXECUTES — a `/mode:X` in a prompt is a command to invoke, not context. Zero new memory files — the fix is in the system. Traces RT-2026-06-08-dispatch-skip-recurrence + RT-2026-06-08-memory-over-system; ED-035.

- [x] **S-13 (stale-reference scrub — operator-raised 2026-06-08: "stale references in several files and folders, including CLAUDE.md and AGENTS.md"):** the ADR-0007 dept-tree rewrite + the S-7 role renames + the `.system`→`_system` de-dots left broken/legacy path and identifier refs in high-read canon. DoD: (a) **fix** the concrete stale refs the operator named + the surrounding class (CLAUDE.md, AGENTS.md, skill-map.md, + any sibling canon); (b) **S-13b** — a NAMED enforcer (`scripts/checks/doc-ref-integrity.js`) that makes a future stale relative-path ref self-detecting (broken-link scan + allowlist for ephemeral/design/placeholder refs + planted-violation test + wired into `/scan:full`), so the class can't silently recur. (a) BUILT 2026-06-08 (`150252b`); (b) tracked below. See the S-13 section.

## Related definitions
- Wiring, Verification, Source of truth, Evidence — see ../../TRACKER.md

## Related sprints
- S-1 audit — Planned (analysis-first; produces the frozen findings)
- S-2 dispatch-guide consolidation — **Partial** (non-destructive content-merge + CLAUDE.md + alpha.md done; hard-move/delete behind dry-run gate)
- S-3 agents-folder de-dot — Planned (absorbs E-DISPATCH-INTEGRITY-001 F-4/F-5)
- S-4 cutover-completeness extension — Planned (absorbs ED-026)
- S-5 canonical scrub + boundary closure — Planned (operator-scoped; absorbs E-BOUNDARY-001)
- S-6 duplicate-drift enforcer — Planned (the self-detecting backstop)

## Dependencies
- None hard. Complements E-DISPATCH-INTEGRITY-001 F-1/F-2/F-3 (coverage-honesty slice). Absorbs E-DISPATCH-INTEGRITY-001 F-4/F-5, ED-026, the Maintainer-canonical-scrub Sprint-10 candidate, and E-BOUNDARY-001.

## Blockers
- None currently recorded. (S-5's repo move is operator-scoped — the framework cannot self-execute it.)

## Risks
- Deleting/moving a canonical file without first proving which copy is current (mitigated by analysis-first S-1).
- Both-layers rename missing the imperative layer (scripts/paths/hooks) — the recurring "cutover covers both layers" bug class; S-3 must repoint specs AND scripts.

## Tracker language convention (anti-deixis) — added 2026-06-08

**Problem.** Session-relative deixis ("this session", "next session", "currently", bare "now", "NEXT ACTION (session N)", "no X this session", "DONE this session") is ambiguous when read LATER: the referent of "this session" shifts to whoever is reading, so a *historical* status like "no execution this session" reads to the next session as a *present instruction* ("don't execute"). The session ordinal ("session 3/4") is also meaningless to a future reader. Confirmed recurring (RT-2026-06-05-stale-cutover-refs-alias-masked; the "why is this STILL not done" visibility gap).

**Rule — tracker prose is ABSOLUTE + state-chained, never bare session-relative:**
1. **Anchor every status to an absolute marker** — an ISO date (`YYYY-MM-DD`) and, where it exists, a 7–40-hex commit hash. Not "this session" → "as of 2026-06-08 @aa60e7e".
2. **State, don't narrate.** Use a State-Model value + a dated receipt. "DONE this session" → "DONE 2026-06-08 @<hash>". "no execution this session" → "Execution: NOT STARTED as of 2026-06-08" (dated past-tense, not a bare negative).
3. **NEXT ACTION is a standing imperative** — no session ordinal as the subject. "NEXT ACTION (session 4): do Y" → "NEXT ACTION (open as of 2026-06-08 @<hash>): do Y". Any future reader executes the same thing; the "as of" date makes staleness visible.
4. **Negatives are past-tense + dated**, never present-tense bare ("had not started as of <date>", not "no X this session").

**Enforcer (named; BUILT report-only — ED-034, reconciled 2026-06-16):** a `session-relative-language` check in `scripts/trackers/validate.js` that flags a banned deictic phrase lacking an absolute anchor (ISO date or 7–40-hex hash) within the same clause; allowlisted exceptions (e.g. inside a fenced quote of an old handoff); ramped report-only → blocking after the existing high-read lines are converted. **It IS built + running** — the `session-relative-language` advisory runs in `validate.js` OUTSIDE the 20 binding checks (19 un-anchored deictics flagged 2026-06-16); the flip-to-blocking ramp remains the open tail (S-10). (The "not yet built" claim above was stale — caught by the verify-don't-inherit sweep.)

## Handoff redesign (S-11) — design 2026-06-08 @9163f77 (build PENDING; RI-006)

**Bug (RI-006).** `scripts/hooks/session-stop.js` is wired to BOTH `Stop` (end of every reply) and `SessionEnd`, sharing ONE once-per-session sentinel (`.session-handoff-done`). `Stop` fires first (after reply #1), writes the handoff + flips the lock, so the `SessionEnd` trigger meant to capture the FINAL state never runs → the auto-handoff freezes at first-pause and is stale by close; `/clear`'s `SessionEnd` likewise skips → `/clear` captures nothing fresh. (Proof 2026-06-08: `handoff.md` mtime 08:15 claimed epic ~80% with the day's finished work listed as TODO, while the on-`main` reality at 17:30 was ~92% / D-3 done / 8 commits.) Since `TRACKER.md` is the curated source of truth, the handoff's redefined job is a **safety net** for tracker-drift + untracked work — carrying state between replies, at crash, at close (incl. accidental terminal close), and across `/clear`.

**Design — two layers.**
- **Layer 1 — cheap deterministic live-state** (`handoff-live.md`): written on EVERY `Stop` + `SessionEnd` (incl. `source:clear`); no LLM (~ms); atomic (temp+rename); SEPARATE file from the rich handoff (the per-turn writer must never touch the preserve-marked `handoff.md`). Content = GROUND TRUTH independent of the tracker: absolute ts + branch + HEAD; commits since `.session-start-commit`; **staged+unstaged files + diffstat** (the untracked-work signal); last 2–3 user asks (from the transcript the hook receives); current `in_progress` task + last action; task-list snapshot.
- **Layer 2 — rich narrative** (`handoff.md`): the AI-analyzed prose; `SessionEnd` + manual (`/session:handoff` / `/session:end`); preserve-marked; close-time only.
- **Coverage model.** A cheap snapshot after every turn auto-covers crash / accidental terminal close / `/clear` / close — all happen AFTER a completed turn that is already on disk, so the system stays ≤1 turn behind any disaster. The ONLY unrecoverable loss is a crash MID-turn (no hook observes an unfinished turn).

**Per-scenario cases.**
- **New session (`source:startup`).** A leftover PRIOR-session `handoff-live.md` is still on disk AND `startup` resets `.session-start-commit` → must NOT present the prior live-state as current (archive it timestamped; surface it only if its uncommitted files still exist on disk). Turn-1 gap: before the first `Stop`, only the prior snapshot exists → label every snapshot by `session_id` + absolute ts so it is never mistaken for current.
- **Resume session (`source:resume`).** `claude --resume <old-id>` into a runtime dir where a NEWER session last ran loads the WRONG baseline/live-state, because `.claude/runtime/` state is keyed to "last session in this folder", not to the resumed conversation → compare the hook payload `session_id` to the on-disk `.session-id`; on mismatch, re-baseline or warn LOUDLY (never silent-fallback). Baseline is correctly preserved on resume already.
- **Post-`/clear` (`SessionEnd(clear)` then `SessionStart(clear)`).** The conversation is GONE after `/clear`, so the handoff is the ONLY bridge and MUST be written BEFORE the wipe (capture at `SessionEnd(clear)` while context still exists) and read back by `SessionStart(clear)` — the current sentinel BLOCKS this write and must be removed. Ordering (SessionEnd write completes before SessionStart read) + atomic write are load-bearing. "/clear to switch tasks" vs "/clear to continue" is ambiguous → load the pre-`/clear` state but LABEL it ("ignore if you cleared to switch tasks"); the uncommitted-files list is useful either way. The baseline keeps counting across `/clear` (frame the result as "prior / pre-`/clear` work in this session", per the anti-deixis convention).
- **Cross-cutting / physics.** `SessionEnd` does NOT reliably fire on hard kill / accidental terminal close → those are covered by the last `Stop` snapshot ONLY (the rich Layer-2 is simply absent after a crash — state that plainly). The in-hook git must be read-only + short-timeout + **fail-open** (an `index.lock` from a concurrent git op must not hang the turn). `runtime/` is gitignored → the handoff never travels cross-machine; cross-machine continuity is the committed `TRACKER.md`'s job (keep the two roles distinct).

**4 LOAD-BEARING acceptance criteria (the S-11 DoD).** (1) **Per-`session_id` keying** — `.claude/runtime/` is single-tenant but the operator runs CONCURRENT sessions → they clobber `.session-id`/baseline/handoff today (a latent bug a per-turn writer AMPLIFIES); key files by the payload `session_id` (`handoff-live-<sid>.md`, `.session-start-commit-<sid>`), loader picks the matching sid else most-recent + labeled. (2) **`SessionEnd(clear)`→`SessionStart(clear)` ordering + atomic write** — makes `/clear` actually carry. (3) **Load-time freshness comparison** — when `handoff-live.md` is NEWER than `handoff.md`, the loader MUST surface the live-state delta ("⚠ uncommitted work newer than the last narrative"), else untracked work is invisible (the exact failure being prevented). (4) **Honest hard-kill framing** — cheap-snapshot-only after a crash; never imply otherwise.

**Build plan (additive-first; each step independently safe).** (1) Layer-1 writer as a NEW per-sid file that does not touch `handoff.md` (cannot break current behavior). (2) Watch across a few real sessions (freshness, no per-turn latency, no clobber). (3) Wire the `SessionStart` loader (freshness comparison) + the `/clear` bridge + RETIRE the freeze sentinel. (4) Fixture test: simulate `Stop`×N / `SessionEnd(clear)` / two concurrent sids / a planted crash-after-turn, asserting the live-state is correct + non-clobbered each time.

**Reuses existing infra (already correct):** `session-start.js` source-branching (`startup`/`resume`/`clear`/`compact`); `.session-id`; `.session-start-commit` (set on `startup` only, preserved across clear/resume/compact, so "commits since session start" already spans a `/clear` correctly). **Full design:** `runtime/notes/handoff-redesign-2026-06-08.md`.

## Persistent-team initialization (S-12) — design + spec (the once-and-for-all team-init fix)

**Bug (recurred 3 surfaces).** 2026-06-06 "where is the team?"; 2026-06-08 "why did you not dispatch the persistent team?" + "this isn't the persistent team I imagined — where's epsilon?" In a `/mode:sprint` session the persistent team must be the named **company faces (α+ε+β)**; twice the team was ABSENT and once it was the WRONG ROSTER (generic `general-purpose` workers, no ε). Traces: `RT-2026-06-08-memory-over-system`, `RT-2026-06-08-teaminit-fixdeep`, `RT-2026-06-08-dispatch-skip-recurrence`.

**Root cause (grounded in the SYSTEM, not memory).** `.claude/commands/mode/sprint.md` is complete and lesson-encoded — Step 1.75 spawns α+ε+β with the exact calls and even pre-states "NEVER defer the team … witnessed 2026-06-06." The correct team-init lives ONLY inside that skippable `/mode:sprint` caller. When a `/clear`'d kickoff names `/mode:sprint` as TEXT, the Skill is never invoked, Step 1.75 never runs, and the team is improvised from a lossy auto-memory summary → absent/wrong roster. **The operating loop permitted injected auto-memory to SUBSTITUTE for the system at the decision point.** Advisory injection alone is insufficient (proof: `session-start.js` already injects a MANDATORY dispatch-guide reference + the handoff on every cold start, and the skip STILL happened).

**Principle (now enforced by Layers a+b, not just doctrine):** *memory POINTS to the system; the system EXECUTES. A `/mode:X` in a prompt is a command to INVOKE, not context.*

**Canonical roster — the RIGHT faces per mode (single source for all three layers):**

| mode | lead | persistent faces to spawn | team name |
|---|---|---|---|
| sprint | α | **ε (epsilon) + β (beta)** | `<slug>-sprint` |
| adhoc | α | **β (beta) + γ (gamma)** | `<slug>-adhoc` |
| oneshot | — | δ IS the session (standalone) — no persistent team to spawn | — |
| solo | — | none | — |

`<slug>` = `path.basename(cwd).toLowerCase().replace(/[^a-z0-9]/g,"")`.

**Fix shape:** system-level, at the action boundary, off persisted `mode.json` (which SURVIVES `/clear`, so it fires REGARDLESS of whether `/mode:sprint` is invoked). Three layers:

### Layer (a) — `session-start.js` MANDATORY system-sourced team-init directive · STATUS: BUILT 2026-06-08 (`456dfd5`)
- **Mechanism:** on `source ∈ {startup, clear}`, read `.claude/runtime/mode.json`; if `mode` is a team-mode (sprint/adhoc) AND the correct team is not live, inject — as `hookSpecificOutput.additionalContext`, FIRST (before all other context) — a `⛔ <MODE> MODE IS ACTIVE` directive carrying the EXACT Step-1.75 calls (`TeamCreate` + one `Agent(subagent_type:<face>, team_name, name, run_in_background, prompt:"STARTUP DIRECTIVE…")` per face, with each face's load list), framed as the MANDATORY first action.
- **Liveness check (also how the WRONG-ROSTER case is caught):** glob `~/.claude/teams/*/config.json` for a config with mtime < 24h whose `members` JSON contains ALL the mode's expected faces (`spec.faces.every(f => membersBlob.includes(f))`). Absent OR missing-a-face ⇒ not live ⇒ directive fires (a team of only generic workers lacks `epsilon` → fires).
- **Safety:** whole block try/catch + fail-open (NEVER blocks session start); additive (cannot break the existing handoff/live-state load).
- **Acceptance criteria — `scripts/hooks/session-start-teaminit.test.js`, 5/5:** (1) sprint + NO team ⇒ FIRES with `TeamCreate`+`epsilon`+`beta`+"FIRST action MUST be"; (2) sprint + team WITHOUT ε ⇒ FIRES (wrong-roster); (3) sprint + team WITH ε+β ⇒ SUPPRESSED; (4) adhoc + NO team ⇒ FIRES with `beta`+`gamma`; (5) solo ⇒ NO directive.
- **Refs:** `scripts/hooks/session-start.js` (`teamInitDirective` block + the inject condition/first injection) + `session-start-teaminit.test.js`.

### Layer (b) — `team-guard.js` PreToolUse-on-Agent dispatch-time ADVISORY · STATUS: BUILT 2026-06-08 (`380025a` + `cede829`)
- **Mechanism:** PreToolUse on `Agent`. After the β/γ-teammate + oneshot/solo short-circuits, if `mode.json.mode === "sprint"`: a dispatch carrying `team_name` OR a research agent (`explore`/`plan`) ⇒ ALLOW (exit). Else (worker, no team_name): find the freshest active `~/.claude/teams/*/config.json`; `teamActive` = exists+fresh; `teamHasEpsilon` = members contain epsilon; `teamReady` = both. Per-session one-off counter (`.claude/runtime/.sprint-oneoff-count`, reset to 0 when teamReady). If `!teamReady && n >= 2` ⇒ emit a NON-BLOCKING `additionalContext` advisory: "no active persistent team — stand up α+ε+β" (if `!teamActive`) OR "a team is active but MISSING ε — spawn `subagent_type:epsilon`" (if active-but-no-ε). NEVER blocks; fail-open.
- **Acceptance criteria — `scripts/hooks/team-guard-sprint.test.js` 8/8 + regression `scripts/test-team-guard.js` 16/16:** planted no-team FIRES; team_name present SUPPRESSED; research one-off SUPPRESSED; solo SUPPRESSED; team-without-ε FIRES (missing-ε); team-with-ε SUPPRESSED; first one-off (ramp) SUPPRESSED; adhoc build-chain still BLOCKS (regression).
- **Refs:** `scripts/hooks/team-guard.js` (the `currentMode === "sprint"` block) + `team-guard-sprint.test.js`.

### Layer (c) — HARD PreToolUse readiness GATE · STATUS: BUILT 2026-06-08, **DEFAULT-ON** (mechanically flipped 2026-06-08; doc reconciled 2026-06-09 — see Change log; code-authoritative)
- **Goal:** make team-init truly UN-SKIPPABLE — BLOCK substantive WORKER dispatch until the correct team is live, while ALLOWING the bootstrap (so the team CAN be stood up).
- **Mechanism (extend `team-guard.js`, gated behind a `HARD_GATE` flag so it ramps advisory→block):** when `mode.json.mode ∈ {sprint, adhoc}` AND NOT `teamReady`, for an `Agent` dispatch —
  - **ALLOW (exit 0):** the faces (`subagent_type ∈ {epsilon, beta, gamma, delta}`), research (`subagent_type ∈ {explore, plan}`), and ANY dispatch carrying `team_name` (so spawning INTO the new team works). These are the bootstrap + read-only paths.
  - **BLOCK (`{decision:"block", reason}`)** from the **1st** worker dispatch (NO ramp — un-skippable, unlike the (b) advisory's `n>=2`): a WORKER (`general-purpose` + the build-chain `GAMMA_ONLY_TYPES`) with NO `team_name`. Reason = "⛔ <MODE> MODE + no correct team live. Stand up the company faces FIRST (the exact `TeamCreate` + `Agent(subagent_type:epsilon/beta…)` calls), then dispatch workers through the team. (E-SYSTEM-ORG-001 S-12c)".
- **Why deferred — false-block risks to handle BEFORE flipping on:**
  1. **Long-session staleness** — the (a)/(b) liveness uses a 24h mtime window; a live team idle >24h would read "not live" → false-block. **Mitigation:** write a sid-keyed `~/.claude/runtime/.team-live-<sid>` heartbeat when the team is confirmed-up (readiness pings received) and check THAT (presence, not config-mtime), OR accept team-config PRESENCE (not freshness) when it carries the faces, scoped by `.session-id`.
  2. **Bootstrap deadlock** — the ALLOW-list (faces + `team_name` + research) MUST be airtight so the gate can never block the calls that satisfy it. Test explicitly (case 2/3/4 below).
  3. **Kill-switch** — honor `WARPOS_DISABLE_TEAM_GATE=1` (env) OR a `.claude/runtime/.team-gate-off` marker so a stuck gate is bypassable without editing the hook.
  4. **Fail-open on ANY hook error** (never block on an exception).
- **Acceptance criteria — `scripts/hooks/team-guard-gate.test.js` (to write):** (1) sprint + no team + `general-purpose` worker ⇒ **BLOCK**; (2) sprint + no team + `subagent_type:epsilon` ⇒ ALLOW (bootstrap); (3) sprint + no team + `explore` ⇒ ALLOW (research); (4) sprint + dispatch WITH `team_name` ⇒ ALLOW; (5) sprint + team WITH ε ⇒ ALLOW worker; (6) sprint + team WITHOUT ε ⇒ BLOCK worker (wrong roster); (7) kill-switch set ⇒ ALLOW; (8) heartbeat fresh but config-mtime >24h ⇒ ALLOW (no false-block). + regression `test-team-guard` stays green.
- **Build order:** add the heartbeat-liveness helper → add the `HARD_GATE`-gated block (allow-list + block) + kill-switch + fail-open → write `team-guard-gate.test.js` (8 planted + the allow cases) → run a watch-period in advisory → flip `HARD_GATE` on once no false-blocks observed.
- **BUILT 2026-06-08 — to-the-t:** `team-guard.js` now carries (1) module-scope `teamHeartbeatFresh(projectDir)` — sid-keyed `~/.claude/runtime/.team-live-<sid>` presence check (fail-CLOSED to "not fresh"; 7-day generous bound; sid-scoping is the real staleness guard) — and (2) inside the `currentMode === "sprint"` block, after `teamReady`, the gate: `hardGate` AND NOT `killSwitch` (`WARPOS_DISABLE_TEAM_GATE=1` OR `.claude/runtime/.team-gate-off`) AND NOT `teamLive` (`teamReady || teamHeartbeatFresh`) ⇒ `{decision:"block", reason:"⛔ SPRINT MODE + no correct team live …"}`. The early ALLOW-list was widened to the FACES (`epsilon/beta/gamma/delta`) + RESEARCH (`explore/plan`) so the bootstrap is airtight (β/γ already exit earlier via `TEAMMATE_TYPES`; ε/δ now exit here). **DEFAULT-ON** (reconciled 2026-06-09 — code-authoritative) — `hardGate = process.env.WARPOS_TEAM_GATE_SOFT !== "1"` (team-guard.js ~L255), i.e. the gate is ACTIVE out-of-the-box; the **SOFT opt-out** (`WARPOS_TEAM_GATE_SOFT=1`) ramps it back to the (b) advisory. The legacy opt-IN (`WARPOS_TEAM_GATE_HARD=1` / `.claude/runtime/.team-gate-hard` marker) is retained ONLY as a belt-and-suspenders force-on — its ABSENCE no longer disables the gate. Evidence: the DEFAULT-ON line in `team-guard.js` + `team-guard-gate.test.js` case "(+) DEFAULT (no flag) + no team + worker => BLOCK (gate ships ON)". Fail-open preserved via the outer try/catch; false-block risk handled by the sid-keyed heartbeat + kill-switch.
- **Acceptance — `scripts/hooks/team-guard-gate.test.js` 11/11** (the 8 spec cases + kill-switch marker-form + `HARD_GATE`-off-stays-advisory + δ-face-bootstrap) **+ regression `team-guard-sprint.test.js` 8/8 + `test-team-guard.js` 16/16** all green.
- **Scoping note (honest, not a defer):** the gate is wired in the **sprint** block only, NOT adhoc — although the design header reads `{sprint, adhoc}`. In adhoc, `general-purpose` is a *legitimate* Alpha research/cognition lane (the regression suite asserts `adhoc: general-purpose → allow`); a hard block on it would manufacture exactly the false-block (#2 bootstrap-deadlock class) the spec warns against, and adhoc's build-chain leak is already covered by the existing `GAMMA_ONLY_TYPES` block. Sprint is where the operator's complaint lives (α improvising `general-purpose` *workers* instead of standing up the ε team) and where "correct roster" (needs ε) is crisply testable. Extending to adhoc would require an adhoc-specific readiness definition (needs γ, not ε) and is left as a tracked follow-up rather than shipped untested.
- **Residual attribution gap (ED-038, honest limit):** S-12c forces a CORRECT team (with ε) to EXIST before workers flow — but it does NOT (and a PreToolUse hook CANNOT) catch the narrower case the operator hit in-session: α dispatching a `general-purpose` worker WITH `team_name` that happens to do BUILD work, bypassing ε's conducting. The type (`general-purpose`, legitimately Alpha's) and `team_name` (legitimately into-team) carry no build-vs-research signal, and ε-conducted-vs-α-improvised is an in-process attribution invisible to the hook (α and ε are the same process). The **structural fix** is that ε conducts build work via the SUBPROCESS runtime (`epsilon-runtime.js → dispatch-claude.js`), which never traverses the harness Agent tool — so a correctly-conducted sprint shows ZERO build-chain workers at the team-guard. A spoofable `.epsilon-conducting` marker was considered and REJECTED as theater (α could touch it; noise until the runtime writes it). Logged as ED-038 with the full detectability analysis; behavioral discipline + the debt entry are the interim. This is a documented limit, not a defer — the operator's NAMED concerns (no-team / wrong-roster) ARE fully enforced by a/b/c.

### Verification commands (re-prove the fix any time)
```
node scripts/hooks/session-start-teaminit.test.js     # Layer a — 5/5
node scripts/hooks/team-guard-sprint.test.js          # Layer b — 8/8
node scripts/test-team-guard.js                       # regression — 16/16
# Layer c (BUILT) — 11/11:
node scripts/hooks/team-guard-gate.test.js
```

**Refs:** ED-035; `L-2026-06-08-dispatch-skip-enforcer-outside-bypassed-caller`; `.claude/commands/mode/sprint.md` Step 1.75 (the system-of-record for the calls + faces); the canonical-roster table above.

## Stale-reference scrub (S-13) — operator-raised 2026-06-08

> "There are stale references in several files and folders, including CLAUDE.md and AGENTS.md. Add to the epic and fix."

**Why this is its own scope item:** the ADR-0007 dept-tree cutover (`00-alex`/`01-adhoc`/… → `president`/`engineering`/…), the S-7 role renames (learner→ops-analyst, stub-scaffold→skeleton-builder), and the `.system`→`_system` de-dots each repointed the *files that moved* — but high-read CANON that merely *cited* the old names/paths drifted silently. A doc citing a dead relative path is invisible until someone clicks it; the [[rename-cutover-covers-both-layers]] bug class, one layer up (the prose layer).

### (a) The fixes — LANDED 2026-06-08 (`150252b`)
- **CLAUDE.md** — `scripts/checks/dispatch-contract.js` → `scripts/dispatch/dispatch-contract.js` (the enforcer moved to the dispatch/ home; the §Dispatch enforcer-pointer was stale). <!-- doc-ref-ignore: this line documents the OLD→NEW fix, so it names the old (now-absent) path on purpose — the doc-ref self-trip class -->
- **AGENTS.md** (5 edits) — `.system.md` → `_system/agent-system.md` (×3 monolith refs); **Learner** → **Ops-Analyst** (`president/ops-analyst.md`, S-7 rename); **Stub-Scaffold** → **Skeleton-Builder** (`engineering/skeleton-builder.md`, S-7 rename); a bare `.system/` → `_system/`.
- **`.claude/project/reference/skill-map.md`** — `.claude/reference/reasoning-frameworks.md` → `.claude/project/reference/reasoning-frameworks.md` (the reference dir moved under `project/`). <!-- doc-ref-ignore: OLD→NEW fix documentation; names the old (now-absent) path on purpose -->
- Method: grepped the OLD literals across all `.md` (per the refactor-hygiene rule) so the fix covered the class, not just the two files the operator named.

### (b) S-13b — the named enforcer (so the class self-detects next time) · STATUS: BUILT 2026-06-08 (REPORT-ONLY ramp, 0-broken baseline)
- **Mechanism:** `scripts/checks/doc-ref-integrity.js` — scan the high-read canon surface (root `*.md` + the `.claude/agents`, `.claude/commands`, `trackers` trees) for relative-path refs in two shapes: markdown links `](path)` and backtick prose refs anchored to a repo top-level dir + extension. Resolve each against the repo ROOT or the citing file's dir; a ref that exists nowhere is a **broken ref**. Pure `evaluate({refs, allowlist})` core (refs pre-annotated with `exists`) for a disk-free bite-test. Allowlist (`doc-ref-integrity.allowlist.json`): `pathPrefixes` (runtime-generated, frozen capsules, removed-by-design dead-tree, `../sprints/SP-` planned backlog) + `literals` (planned/historical/generated scripts + generated maps) + per-line `<!-- doc-ref-ignore[: reason] -->`. Fail-closed (runner error = exit 2); REPORT-ONLY by default, `--enforce` → exit 1.
- **Built to-the-t:** the enforcer SURFACED the operator's complaint at true scope — **168 broken refs** across canon (not just CLAUDE.md/AGENTS.md). Resolved ALL to a **0-broken baseline**: (i) two real extractor soundness bugs fixed (a `tests/transcripts/x.md` substring false-match → word-boundary lookbehind; the `foo.*`/placeholder + ignore-mark filters not applied to backtick refs → applied); (ii) ~10 GENUINE navigational stale-renames FIXED in live docs (`reasoning/run.md`, `skills/cleanup|delete.md`, `agent-dispatch-guide.md`, `oneshot/preflight.md`, `qa/check.md` → the moved `project/reference`, `scripts/dispatch`, `engineering/skeleton-builder`, `product/quality/qa-reviewer` homes); (iii) the rest categorized into the allowlist or `<!-- doc-ref-ignore -->`'d on self-documenting / negative-condition / OLD→NEW-fix-description lines (the enforcer-doc-self-trip class). Final: `326 canon docs · 2060 refs · 0 broken · 130 allowed-absent`.
- **Tests:** `scripts/checks/doc-ref-integrity.test.js` **14/14** — PURE evaluate, extractRefs (md-link + anchored-backtick + external/placeholder rejection + ignore-mark both forms), a LIVE `run()` ok:true on real canon, and a LIVE-PLANTED sealed-repo dead link caught end-to-end (report-only exit 0, `--enforce` exit 1). Wired into `/scan:full` report-only alongside duplicate-doc-drift.
- **Why a real enforcer, not a one-time grep:** the operator hit this AFTER three separate rename waves each "swept the refs" — the recurrence IS the evidence that a manual sweep doesn't hold. The enforcer is the structural answer (every policy needs a named enforcer).

## Change log
- <!-- doc-ref-ignore: the dead path scripts/dispatch/dispatch-skill.js is QUOTED deliberately below to document the prose correction --> 2026-06-10 (tail item (a) dispatch-skill safe-spawn wire-through — DONE-reconcile, ε verify-before-build) — the Current-state + Percent-completion prose listed "dispatch-skill safe-spawn wire-through (last raw spawnSync path)" as REMAINING, but it was already LANDED 2026-06-08 in `150252b` ("feat(system-org): tail — … + dispatch-skill wire-through + §13.7 harness") — the same downstream-version-drift class as Lane A's AL-W-007 already-fixed (ED-008). A new conduct instruction to build it triggered a verify-before-build check (verify exact file/line first) that found the premise stale; no SP was minted. **Two corrections:** (1) the path prose was wrong — the file is `scripts/dispatch-skill.js` (TOP-LEVEL, peer of dispatch-claude.js/dispatch-agent.js), NOT `scripts/dispatch/dispatch-skill.js` (which does not exist); fixed in-prose. (2) marked tail (a) DONE @`150252b`. **Evidence (verified at main @a05b1ef):** `scripts/dispatch-skill.js` PRODUCTION spawn routes through `safeSpawn.safeSpawnFile("claude", …)` (~L494) — claude abs-path-resolved, arg-allowlisted, stdin-normalized, tree-kill, cmd.exe/c for the .cmd shim; FAILS CLOSED if the kernel is missing (~L477-483, refuses the legacy shell spawn, exit 2); §17.4 record stamping intact (argv_schema_version + cwd + output_digest, ~L611-613); the only raw `spawnSync` left (~L433/L453) is inside the `if (usingSeam)` DISPATCH_SKILL_BIN TEST-SEAM (shell:false, injected fake-node bin), mirroring dispatch-claude.js exactly — NOT a production injection surface. Tests green: `scripts/dispatch-skill.test.js` 11/11 (incl. test #11 asserting the SAFE_ARG_RE gate fronting the kernel's claude arg-allowlist — refuses `x;calc`); `scripts/dispatch/safe-spawn.test.js` 27/27 (18 planted). No raw production `shell:true` remains in the dispatch layer (grep across dispatch-skill/claude/agent + dispatch/*.js + providers.js: only the test-seam spawn + a comment in providers.js's fail-closed refusal). **Epic state UNCHANGED** otherwise — still Active; the two operator-gated/bounded items (§13.7 instrumented A/B measurement + S-5 boundary scrub) remain the open work. Docs-only reconcile; no code touched.
- 2026-06-09 (S-12c DEFAULT-ON doc-drift reconcile — done under E-LIFECYCLE-001 / S-LC-01, P-057 two-surface, code-authoritative) — the epic prose repeatedly claimed the S-12c hard team-readiness gate "ships DEFAULT-OFF / `HARD_GATE` flip is the ramp tail", but the CODE was mechanically flipped to **DEFAULT-ON** on 2026-06-08 (operator directive, see the team-guard.js comment block) and the docs were never updated — a pure documentation drift. Reconciled both surfaces to DEFAULT-ON, code-authoritative. **Evidence:** (1) `scripts/hooks/team-guard.js` ~L255 `const hardGate = process.env.WARPOS_TEAM_GATE_SOFT !== "1";` (the gate is ACTIVE unless the SOFT opt-out is set); (2) `scripts/hooks/team-guard-gate.test.js` case "(+) DEFAULT (no flag) + no team + worker => BLOCK (gate ships ON)" — 13/13; (3) the `.team-gate-hard` marker is now a legacy belt-and-suspenders force-on, its ABSENCE no longer disables the gate. Updated the DoD S-12 entry, the Layer-(c) status header + BUILT line, the Current-state line, and the historical 2026-06-08 S-12 Change-log entry (annotated, not silently re-dated). The gate's DEFAULT-ON BEHAVIOR was NOT changed by this reconcile — docs only. Mirror entry in `TRACKER.md` § Change Log.
- 2026-06-08 (SP-20260608-001 — THE DISPATCH-SHAPE NORTH STAR, ε-conducted via a real `/sprint:full`, merged `06fee13` + pushed) — the actual PLAN-§17 point of the epic, finally built (operator: "you didn't even implement the dispatch shape"). Run through the real sprint machinery (ε conducted the roster, β gated all 4 boundaries + DIRECTIVE-resequenced the tickets). **5 tickets DONE:** (T-269) §13.6 ping-reap UNBLOCK — `safeSpawnFile()` (durable-file redirect survives the RI-004 foreground reap + savepoint + bounded retry), N=12 real runs 0 reaps; (T-270) the live shape **RESOLVER** `scripts/dispatch/dispatch-shape.js` — α drafted it solo, **the gauntlet found it WRONG (2 real bugs: read `subprocess_verified` vs the contract's real `verified` → every proven skill routed inline forever; + a downgraded-unproven falsely `proven:true`), both fixed** (β's keep-and-verify vindicated); `dispatch-shape.test.js` 26/26 incl. a live-mismatch test with teeth; (T-271) `resolveShape`/`shapeMismatch` wired into the dispatch wrappers as the consulted self-detection authority (a wrong-shape dispatch FIRES the advisory); (T-272) real §13.6 ping + earn-it — fail-closed, **ZERO skills stamped** (honest: scan:full's real runtime exceeds the bounded smoke; §13.7 full A/B stays operator-gated); (T-273) close-out (enforcers green, manifests regen, suites green). **Process correction:** α kept skipping ε / hand-building — corrected mid-sprint to ε-conducts-the-roster; /fix:deep on the recurring class → `RT-2026-06-08-dispatch-class-rca` (root: orchestration invariants are memory-enforced not GATED; cure = the `_planning/ingest/mc-lifecycle.md` mode:init:gate epic). Residual **ED-039** (dispatch-claude.js production still uses buffered safeSpawnSync → long roster builds can reap; follow-up). RI-001 close (ff-merge, no release-build, retro deferred).
- 2026-06-08 (S-11 Layer-2 — sentinel-retire, RI-006 direct fix) — `session-stop.js` retired the `.session-handoff-done` freeze sentinel; the rich narrative handoff is now trigger-gated to SessionEnd (close/`/clear`/logout) + manual, returning early on end-of-turn Stop/StopFailure (Layer-1 covers those). Removes the freeze (first Stop wrote+locked → SessionEnd skipped) + revives the `/clear` bridge; close-time cleanup of session-scoped files moved off the first-Stop. Watch-period substituted with `session-stop-sentinel.test.js` 7/7 (incl. the NO-FREEZE Stop×3→SessionEnd assertion + preserve-marker). S-11 now FULLY built (Layer-1 + Layer-2).
- 2026-06-08 (S-13 — stale-reference scrub, operator-raised + S-12c hard-gate landed) — operator: "stale references in several files and folders, including CLAUDE.md and AGENTS.md. Add to the epic and fix." **(a) Fixes LANDED `150252b`:** CLAUDE.md dispatch-contract path (→ `scripts/dispatch/`), AGENTS.md ×5 (`.system.md`→`_system/agent-system.md` ×3 + Learner→Ops-Analyst + Stub-Scaffold→Skeleton-Builder), skill-map.md reasoning-frameworks path (→ `project/reference/`); grepped OLD literals across all `.md` to cover the class. **Now documented in the epic** (this commit) — the operator's "add to the epic" was outstanding. **(b) S-13b enforcer** (`doc-ref-integrity.js`) tracked in the S-13 section. **Also this commit:** S-12c hard team-readiness gate flipped DEFERRED→BUILT in the DoD + Layer-(c) + S-12 entries (the gate itself shipped `394b696`). NOTE: the slug-leak naming caveat from the D-2 entry still applies to this file (don't name client slugs in tracker prose — re-trips framework-purity).
- 2026-06-08 (D-2 — `president/.system` → `president/_system` de-dot, ε-conducted) — the LAST both-layers rename in the epic. α built foreground per the D2-dedot-plan §4: git mv the dir; 19 registry-source keys + walk-skip glob → `_system` + `paths/build.js` regen (keys verified survived in generated `.claude/paths.json`); 72-file sweep of imperative+doc citers (3 registry-bypassing fallbacks, all delta/oneshot raw-literal scripts incl. logger.js:62, hooks/checks/gates, the test fixture, root canon + agent specs + skill docs); added the `{literal:"president/.system"}` DEAD_TREE needle to cutover-completeness.js (self-allowlisted, no self-trip); updated the 2 allowlist keys + `.gitignore` glob; regen maps + framework-manifest (BC-02 honest). **ε BINDING gauntlet:** cutover-completeness 0-live-stale (live needle), test-hook-points 43/0 + test-dispatch-agent-resolution 24/24 (the REAL both-layers catchers — bijection trio NOT the criterion), paths source→generated verified, live oneshot-δ probe reached `_system`. **Cycle-0 FAIL** (framework-purity: D-2 SURFACED a pre-existing product-slug leak in 4 framework files by bringing them into the diff) → α **SCRUBBED** (incidental example slugs → generic; the ADR-0001 brand → `DevRepo` + a provenance note; scrub-not-allowlist to keep the purity gate strong) → **cycle-1 PASS**. (Note: the slug literal itself is deliberately NOT named here — naming it would re-trip framework-purity on this tracker file, the enforcer-doc-self-trip class.) **β DECIDE 0.88** (safe to ff-merge+push; `backup/pre-cutover-2026-06-04` is the irreversibility mitigation). RI-001 close: ff-merge + ledger, no release-build, retro deferred. Residuals (NOT D-2-attributable, HEAD-identical): RI-003 release-convergence (`_mc/MANIFEST` + framework-installed lag `.system`, converge at /mc:release); test-sprint 15v10 + cutover.test#11 stale fixtures; + **ED-037** (delta-scripts un-caught JSON.parse on absent store.json — pre-existing hardening follow-up, plan §2A-c).
- 2026-06-08 (S-12 — once-and-for-all persistent-team init, operator-escalated) — the team-skip recurred a 3rd surface; operator: "you always operate from memory and ignore the entire WarpOS we've built for 3 months." Deep trace GROUNDED IN THE SYSTEM (`.claude/commands/mode/sprint.md` was complete + had the exact lesson pre-encoded; I bypassed it by never invoking `/mode:sprint` — treated it as `/clear`'d-handoff text) + /reasoning:run + dry-run /fix:deep (RCA, 5 solutions) → ROOT = the operating loop permits injected auto-memory to SUBSTITUTE for the system at the decision point; the correct team-init lives only inside the skippable `/mode:sprint` caller. Fix (system-level, off persisted `mode.json`, fires regardless of `/mode:sprint` invocation): (a) `session-start.js` mandatory system-sourced Step-1.75 directive with the RIGHT faces (5/5 `session-start-teaminit.test.js`) — BUILT; (b) `team-guard.js` dispatch-time advisory incl. wrong-roster/missing-ε (8/8 + regression 16/16) — BUILT; (c) HARD PreToolUse readiness-gate — **BUILT 2026-06-08** (`team-guard.js` sprint block; heartbeat-liveness + kill-switch + fail-open; DEFAULT-ON [mechanically flipped 2026-06-08; doc reconciled 2026-06-09 — `hardGate = WARPOS_TEAM_GATE_SOFT !== "1"`, code-authoritative]; `team-guard-gate.test.js` 13/13 + regressions green; the long-session false-block risk is handled by the sid-keyed heartbeat). **Zero new memory files — the fix is in the system.** See the S-12 DoD entry.
- 2026-06-08 (continuation — execution by the persistent team `mc-sprint`: lead + reviewer + builder) — **4 streams DONE.** (1) **§14 integration-seam extraction:** extracted the producer/consumer 3-layer seam model from the `_system/agent-system.md` archive §14 into a focused `_system/guides/integration-seam-contract.md` (no `name:` frontmatter; ADR-0007 ownership; illustrative resume-builder example fenced); repointed the 3 §14 citers (backend/builder, director-of-engineering, security-lead); monolith banner + §14 section declare it extracted. Gates: cutover-completeness 0, duplicate-doc-drift 0, no-nul-bytes, broken-refs clean. (2) **S-11 handoff redesign — Layer-1 safety net:** new per-sid `scripts/hooks/handoff-live.js` (cheap deterministic git ground-truth snapshot on every Stop/SessionEnd/StopFailure; atomic temp+rename; read-only short-timeout fail-open git; per-sid `.session-start-commit-<sid>` baseline immune to session-stop's cleanup; **hardened porcelain parser — fixed a real git-for-windows 2-char-prefix leading-dot bug**); SessionStart loader does the load-time freshness comparison + `/clear` bridge + per-source labeling; wired BEFORE session-stop via `_mc/settings/defaults.json` recompile (compile self-test 31/31); 13/13 fixture `handoff-live.test.js` + 0.24s live smoke. All 4 acceptance criteria met. Layer-2 sentinel-retire DEFERRED. (3) **S-10 deixis enforcer:** `session-relative-language` advisory in `scripts/trackers/validate.js` (flags un-anchored deixis; allowlist; report-only OUTSIDE the 20 binding checks → exit-0 preserved; 70/70 selftest incl. 3 P5 cases); convention propagated to TRACKER.md § How to Use; high-read lines converted. ED-034 build closed (flip-to-blocking = ramp tail). (4) **§13.6 skills:test earn-it harness:** `dispatch-skill.js --resolve`/`--no-ping` token-free mode + `scripts/skills-test.js` (registry-driven smoke; atomic stamp on ping-PASS only; fail-closed; inline-required correctly fails) + 8/8 P5 fixture `scripts/skills-test.test.js` (mutation-verified) + §17.5 benchmark-pack scaffold; live registry NOT mutated, 0 real skills falsely stamped; §13.7 heavy A/B measurement honestly deferred. **Process:** persistent team `mc-sprint` replaced fire-and-forget one-off agents (operator-corrected mid-session — see [[feedback_dispatch_persistent_team_in_sprint_sessions]]); the 3 additive streams ran in parallel as background sub-agents (disjoint file scopes, no-commit lean-return), the S-11 keystone built foreground (highest blast radius). New enforcement-debt: persistent-team-norm (no auto-enforcer) + §13.6 anti-rot stamp-invalidation (skill-catalog-regen not yet revoking `subprocess_verified` on skill edit).
- 2026-06-08 (PM, post-D-3 design conversation) — added two operator-raised solves as tracked scope: **S-10** (anti-deixis tracker-language convention + named `session-relative-language` enforcer + ED-034; landed `9163f77`) and **S-11** (the handoff redesign — two-layer safety-net live-state, full new/resume/post-`/clear`/crash case coverage + 4 load-bearing acceptance criteria; design only, build PENDING; bug = RI-006; full design in `runtime/notes/handoff-redesign-2026-06-08.md`). Decision: do NOT build S-11 at this session's tail — a per-`Stop`/`SessionEnd` hook is the highest-blast-radius change class (hits all future sessions) and warrants a fresh-context, additive-first build behind the 4 criteria, with a fixture test.
- 2026-06-08 (PM, session 3) — **EXECUTION session 3 — 4 commits landed on `main` (pushed).** Operator-authorized full autonomy ($1000, push/commit/merge; turbo permission-layer DENIED by the auto-classifier as too-broad a standing self-grant — proceeded with the speed cadence + per-action approvals instead, NOT reshaping the denied command). Verified green at start (validate.js 20/20 + 8 blocking enforcer gates + catching-gate tests). (1) **`05ff3c3`** — **(b) dispatch-claude.js safe-spawn wire-through:** the build-chain Claude spawn now routes through `safeSpawnSync` (claude resolves to a native `claude.EXE`, shell:false; arg-allowlisted — `--agent b;calc` refused pre-spawn; stdin normalized; whole-tree kill; fail-CLOSED if the kernel is missing). Test seam stays direct (mirrors providers.js Wave-1). VERIFIED: structural probe + LIVE probe (real claude → `ok:true` PROBE_OK 6.8s + well-formed record) + dispatch-claude 9/9 + safe-spawn 22/22. auth-resolver N/A (local claude = OAuth, not an injected key). (2) **`9509569`** — deterministic maps regen (`regen-maps.js`) + both-manifest convergence (the verify-green freshness step; committed separately via stash so the deliverable stayed clean). (3) **`dc6243d`** — **(c) coverage-gate §17.4 + FLIP-TO-BLOCKING:** "a record's existence ≠ covered" — added `ARGV_SCHEMA_VERSION` keystone in dispatch-contract.json (anti-stale/backfill), `output_digest` proof-of-artifact + `cwd` stamped on ALL 3 wrappers (dispatch-agent + dispatch-claude + dispatch-skill); `evaluate()` now requires current-schema + artifact-proof, with optional named-artifact ON-DISK verify (`--verify-artifacts`) + an AUDITABLE `waiver{reason}` escape; coverage-gate is BLOCKING by default (`--report-only` opts out; runtime gate, per-phase, not the static scan). VERIFIED: coverage-gate 14/14 (10 planted — blind-coverage/stale-schema/no-schema/unauditable-waiver/missing-artifact) + a live dispatch stamping argv_schema_version/cwd/output_digest + the BLOCKING gate greening on a real record (exit 0) and blocking a missing role (exit 1). scan/full.md prose updated. (4) **`e0af05e`** — **(a-partial) dispatch-skill §17.4 gate-compliance:** dispatch-skill records now stamp the §17.4 fields (the flip would otherwise reject every skill coverage record). Manifest-coupling handled by regen-both-manifests per commit. **Honest scoping:** the full §13.6/13.7 earn-it is a SUB-SYSTEM (bounded resolve/--no-ping smoke mode + §17.5 benchmark pack), NOT a one-liner — the 6 subprocess skills are all heavy-by-design so a full `--force-subprocess` run is expensive + can't honestly stamp `subprocess_verified:true` (the 119/119-untested-rot anti-pattern); deferred to a tracked follow-up. **D-3 DONE** (after the operator re-invoked `/session:turbo` "full grant" — turbo's permission-layer self-grant was again classifier-denied, so proceeded on the speed cadence + per-action approvals): **`ef3fcf8`** extracted the focused authoritative `_system/guides/gauntlet-contract.md` (parallel-gauntlet/snapshot-diff/unified-fix-brief/snapshot-hashing/escalation-ladder/circuit-breaker/context-scoping; role names modernized to ADR-0007; Evaluator/Compliance kept as gauntlet review-FUNCTION labels with a mapping note; no `name:` frontmatter so findAgentSpec doesn't enumerate it) — delegated the heavy read+author to a lean-return sub-agent, verified the output. **`7ee1488`** repointed the ~7 active citers off the archive monolith (frontend/backend/security builders, security-lead, director-of-engineering ×2, gamma, qa-reviewer, frontmatter-guide + the dispatch-claude.js worktree-isolation comment → agent-dispatch-guide.md §10). **Nuance handled (not a blind swap):** 3 citers also cited the monolith's §14 producer/consumer integration-seam rules, which D-3's scope did NOT extract — those refs were kept pointed at the `agent-system.md` archive §14, with only the Dark-Factory/gauntlet half moved to gauntlet-contract.md; the monolith banner now declares D-3 DONE + flags §14 as the one remaining un-extracted live mechanism. The 2 `president/_system/oneshot/*` citers (stale dotted paths) were left for D-2 (deferred dir). Gates: cutover-completeness 0, duplicate-doc-drift 0, dispatch-claude 9/9, test-dispatch-agent-resolution 24/24, coherence gamma-stale-path passes, no broken refs.
- 2026-06-08 (PM) — **EXECUTION session 2 — Waves 1→2d landed on `main` (pushed).** Operator-authorized full autonomy ($1000, push/commit/merge). (1) **Wave 1** `6db8816` — wired the safety kernel + N-3 auth-resolver into `providers.js` (the LIVE cross-provider spawn path; was new-code-only/zero-callers) — fail-closed, LIVE-PROVEN codex+gemini (PROBE_OK); + S-6 duplicate-doc-drift enforcer + S-8 file-usage-trace tool + S-2 content-merge. (2) **Wave 2a** `cfbadfa` — de-dot `.system/` dir + hard-move the enriched guide → `_system/guides/`, delete the stale orphan (forcing case RESOLVED: duplicate-doc-drift 0-drifted), repoint ~12 consumers via paths.registry source→regen. (3) **Wave 2b** `ace09f2` — de-dot the `.system.md` monolith → `_system/agent-system.md`, repoint 9 citers, scrub a downstream-product slug the de-dot exposed (literal redacted — see framework-purity guard). (4) **Wave 2d** `1c3a9ad` — N-2 provider-api-policy enforcer (live repo CLEAN) wired report-only; §13 dispatch-skill.js + skill-weight registry (delegation infra, 206 skills classified); grandchild-kill fixture (safe-spawn 22/22). Delegated 4 builders via harness Agent-tool (S-6/S-8/N-2/§13/grandchild) per the dispatch-guide §9 "general-purpose authoring + lean-return" lane. All gates green throughout (manifest-honesty 1147, role-parity, cutover-completeness, dispatch-routing-parity, test-dispatch-config, framework-purity). Process note: a `git add -A` swept a sub-agent temp file (`TEMPpapj.json`) into `ace09f2`; removed in `f85adbd` — switched to explicit staging. **S-7 correction (`0061b6a`/`8ee5550`):** the first S-7 merge (`a071e80`) was PREMATURE — landed on the bijection gates (role-parity/dispatch-routing-parity/test-registry-roles all green) while the imperative-layer sweep was incomplete; `test-hook-points` (41/2) + `test-dispatch-agent-resolution` (21/2) FAILED on it (the `learner@retro` sprint-hook-points keystone + the moved `_system/*` spec paths). This is a LIVE re-occurrence of [[rename-cutover-covers-both-layers]] — the green bijection MASKED the un-migrated runtime. The S7renames sub-agent caught it and completed the 16-file imperative sweep (org-roles `STATIC_GAMMA_ONLY_AUGMENT` gate-hole, dispatch-claude `BUILD_CHAIN_ROLES` skeleton-builder worktree isolation, route-guard normalize, sprint-hook-points keystone, +12). Lesson: for a both-layers rename, the catching gates are `test-hook-points` + `test-dispatch-agent-resolution`, NOT the bijection trio — and don't merge a delegated rename until the sub-agent's completion envelope, not when files merely "look touched." Follow-up: preflight.md still dispatches the legacy `stub-scaffold` + points at the moved spec path (works via alias; skill-body update tracked).
- 2026-06-07 — Epic created (operator-directed to TOP). Consolidates the dispatch-guide duplication finding + the scattered org/`.system` cleanup tasks (E-DISPATCH-INTEGRITY-001 F-4/F-5, ED-026, canonical-scrub, E-BOUNDARY-001) into one owned epic.
- 2026-06-08 (PM) — **EXECUTION session 1 — the additive dispatch-shape FOUNDATION (PLAN §17.2 steps 1–3, all additive).** Built + green-tested, nothing destructive, nothing committed-then-broken:
  - **P5 fixture harness** (`scripts/checks/lib/fixture-harness.js`) — sealed-fixture + planted-violation convention; meta-enforces "no enforcer test without a planted violation that fails" (PLAN §12 P5).
  - **N-3 auth-resolver** (`scripts/dispatch/auth-resolver.js` + test) — in-code dotenv parsing (kills the `export $(grep .env|xargs)` injection, §16.2), full source precedence (override key-file → env → .env.local → .env → ~/.gemini/.env → OAuth), labels-not-values, BOM-safe, lists checked sources, verify-before-declaring-available probe seam.
  - **Safety kernel** (`scripts/dispatch/safe-spawn.js` + test) — trusted tool-ID→absolute-path (model never picks the exe; PATH/PATHEXT-hijack guard), input-gate flag/subcommand allowlist (not just metachar refusal), encoding/newline normalization, tree-kill. §16.3/§17.3.
  - **Dispatch-contract KEYSTONE** (`.claude/agents/_org/dispatch-contract.json` + `scripts/dispatch/dispatch-contract.js` + test) — the §17.1 dispatch analogue of role-registry.json; per-role/skill allowed shapes·tool·cwd·file-scope·env·secret·timeout·budget·output·coverage, role class DERIVED from the registry. `validateDispatch` encodes the 3 operator failures as violations (API-when-CLI / in-process-when-subprocess / skipped-coverage). Consumer: dispatch-claude.js consults it (report-only).
  - **N-1 run-ledger + coverage gate** (`scripts/dispatch/coverage-gate.js` + test; `run_id`/`phase_id`/`plan_item_id`/`prompt_digest`/`shape`/`tool_id` stamped on BOTH wrappers' completion records) — a backing `ok:true` record is the precondition for "covered" (kills RC-2 sprint theater); report-only (§4 ramp).
  - **Cross-provider security review** (GPT-5.5 via dispatch-agent.js — dogfooding the dispatch-shape thesis): returned **FAIL** with a real CRITICAL (assertArgs skipped the universal injection check on consumed flag VALUES → `-o ...x&calc` reached `cmd.exe /c` for a .cmd shim, a CVE-2024-27980 bypass) + a HIGH (`-o` temp prefix-match `TempX`⊃`Temp`) + 2 MEDIUM (PATHEXT attacker-controlled; validateDispatch should reject build_chain→in-process independent of the contract). ALL FIXED + permanently guarded with new planted-violation tests; re-reviewed.
  - **Wiring (report-only):** `node scripts/dispatch/dispatch-contract.js validate` added to `/scan:full` (Dispatch-shape integrity gate). scan-coverage + dispatch-routing-parity green.
  - Tests (re-measured on disk 2026-06-08-pm; counts grew after the security-review planted tests were added — the earlier "17/17 + 15/15 / 30 planted" was the pre-fix count): fixture-harness (harness, no standalone test) + auth-resolver 11/11 (5 planted) + safe-spawn 21/21 (16 planted) + dispatch-contract 16/16 (9 planted) + coverage-gate 7/7 (5 planted) = **55 assertions / 35 planted-violations**. Existing dispatch tests unbroken (dispatch-claude 9/9, gauntlet-verify, test-registry-roles 23/23).
  - **GROUNDING PASS 2026-06-08-pm (this session):** verified all 6 artifacts exist + tests green; confirmed the route-through gap is REAL — `safe-spawn`/`auth-resolver` have ZERO callers (grep across scripts/** hits only their own defs); `scripts/hooks/lib/providers.js` (the actual provider spawn path) requires none of them; dispatch-contract wired report-only (dispatch-agent.js:472, dispatch-claude.js:228); coverage-gate standalone-CLI-only. `/scan:full` runs `dispatch-contract validate` (critical-on-nonzero) but the N-1 coverage gate is REPORT-ONLY (scan/full.md:97).
  - **Next:** S-2 dispatch-guide consolidation + the §17.2 destructive tranche (de-dot, renames) behind a full isolated dry-run (§14 + §17.6 preconditions); flip the report-only enforcers to blocking (§4 step 8); refactor the existing wrappers/providers.js to ROUTE THROUGH safe-spawn + auth-resolver (currently new-code-only).
- 2026-06-08 (PM+) — **Wave 1 — S-2 non-destructive content-merge + CLAUDE.md/alpha.md doctrine.** All additive, nothing moved or deleted:
  - **Canonical guide enriched** (`.claude/project/reference/agent-dispatch-guide.md`): folded in orphan's still-useful content (headless provider setup table with codex auth + gemini GEMINI_CLI_TRUST_WORKSPACE; concurrency caps §5; prompt assembly / mandatory-inlining rules §6; worktree isolation + continuation pattern §10; identity-gating §11; thinking-model tool_choice constraint §12; output-handling §13); de-staled orphan bits (corrected `gemini-3.1-pro-preview` from "ghost 404" to live default; updated role names to ADR-0007 — `ops-analyst`, `skeleton-builder`, `cabinet`; removed `gpt-5.4-mini` model refs); added §1 CLI-vs-API policy section + §16 mandatory-reads scope note (covers consults + ad-hoc reviewers, not only build-chain). Grew from 197 to ~290 lines. <!-- doc-ref-ignore: historical S-2 change-log; names the pre-consolidation guide location on purpose -->
  - **CLAUDE.md `## Dispatch` section added** (§10.3 propagation): CLI-vs-API fundamental rule, shape-rule table (build-chain/cross-provider/heavy-skills/research/blocked), orchestrator-holds-envelopes doctrine. Always-loaded — closes failure-iii (banner was ephemeral; doctrine is durable).
  - **CLAUDE.md `## Tool Use` section added**: Grep-glob false-negative trap (confirmed bug class), correct usage table, Edit discipline (read-before-edit), dot-dir behavior, enforcement debt pointer ED-033.
  - **alpha.md `## Dispatch` section added**: guide pointer + key rules (CLI mandatory; build-chain wrapper; cross-provider via dispatch-agent; Agent-tool scope; envelopes not content). Closes failure-iii missing-pointer for ad-hoc Alpha dispatch.
  - **AGENT_GUIDE.md:45 repointed**: was stale orphan path; now canonical `paths.agentDispatchGuide` path.
  - Destructive tranche (hard-move + delete orphan) remains deferred behind dry-run gate.
- 2026-06-08 (AM) — **Design + analysis session (planning only).** Full analysis + design lives in **`runtime/agent-system-plan/PLAN.md`** (§0–§17) with `GO.md` as the execution entry point. The epic was **reframed by the operator into the broader "MC dispatch-shape reliability system"** — agents + skills are one problem (best dispatch shape per unit). Added scope folded into the design doc (this epic remains the TOP/first concrete workstream): a machine-readable **dispatch-contract keystone** (PLAN §17.1, the highest-leverage piece); **skill-execution-routing** with an earn-it ladder (runs §13.6 / pays+good §13.7 / benchmark §17.5); **N-1/N-2/N-3** dispatch integrity (binding coverage gate + CLI-vs-API policy + auth-resolver); **tool-use reliability** (the Grep-glob false-negative, ED-033) + the **isolated-testing-for-everything** mandate; role renames `learner→ops-analyst`/`stub-scaffold→skeleton-builder`/`consult+advisor→cabinet`; **S-8 file-usage trace** + **S-9 Dispatch-Console GUI refresh**; a **Phase-0 feasibility+isolated-dry-run gate**; and the o3 deep-research + GPT-5.5 (×2) hardening (PLAN §16/§17). **Corrected sequencing (PLAN §17.2): the safety kernel + dispatch-contract + fixture-harness + file-usage-trace precede any destructive rename/delete.** Evidence: PLAN.md, `runtime/research/dispatch-subprocess-safety/openai-report.md`, the two `gpt55-*-output.json` consults. Current next action → run PLAN §14 Phase 0 on the operator's "go".
