{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mc/skill-dispatch/benchmark-task-set/v1",
  "title": "Skill-dispatch §13.7 benchmark task set (SCAFFOLD — no measurements shipped yet)",
  "description": "A replayable task set that proves subprocessing a skill is WORTH IT (Axis 1: token/context savings) AND its results are GOOD (Axis 2: quality >= inline within tolerance). PLAN §13.7 + §17.5. A skill earns `execution: subprocess` only when BOTH axes pass, measured per-skill, never assumed. This schema describes the INPUT (tasks + thresholds); run artifacts/verdicts live under runs/ and results/.",
  "type": "object",
  "required": ["schema", "skill", "thresholds", "tasks"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": "mc/skill-dispatch/benchmark-task-set/v1" },
    "skill": {
      "type": "string",
      "description": "The skill under measurement, e.g. 'scan:full' or 'research:deep'. Must already be §13.6 subprocess_verified (a ping smoke proved it RUNS headless) before §13.7 measurement is meaningful."
    },
    "subprocess_verified_prereq": {
      "type": "boolean",
      "description": "Asserts §13.6 was passed (skills:test --mode ping stamped it) before this task set was authored. §13.7 depends on §13.6.",
      "default": false
    },
    "thresholds": {
      "type": "object",
      "description": "The fail-closed bars BOTH axes must clear. A skill qualifies for subprocess ONLY when all are met across the task set.",
      "required": ["min_tokens_saved", "min_pct_saved", "quality_tolerance"],
      "additionalProperties": false,
      "properties": {
        "min_tokens_saved": {
          "type": "integer",
          "minimum": 0,
          "description": "Axis 1: minimum net orchestrator tokens saved (inline_orchestrator_tokens - (envelope_tokens + dispatch_overhead)). Light skills whose output is already small fall below this and STAY INLINE."
        },
        "min_pct_saved": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Axis 1: minimum fraction of inline orchestrator cost saved (e.g. 0.6 = the envelope must cost <=40% of inline)."
        },
        "quality_tolerance": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Axis 2: how far BELOW inline quality the subprocess result may fall and still pass (e.g. 0.05 = within 5%). 0 = subprocess must be >= inline."
        }
      }
    },
    "judge": {
      "type": "object",
      "description": "Axis 2 quality grader. Independent + cross-provider (no self-grading — diff-model-review, GPT Pro §9.4). Used for the SUBJECTIVE delta and high-risk routing/calibration; where a gold expectation exists, grade against it FIRST (§17.5: the judge is not the sole oracle).",
      "additionalProperties": false,
      "properties": {
        "provider": { "type": "string", "description": "A provider DIFFERENT from the skill's executor (cross-provider)." },
        "anonymized": { "type": "boolean", "description": "Arms presented to the judge WITHOUT inline/subprocess labels (blind A/B)." },
        "rubric": { "type": "string", "description": "What 'good' means for this skill's output." }
      }
    },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "description": "Representative inputs replayed under BOTH arms (inline + subprocess). A single run is insufficient (§17.5) — several tasks average out retries/summarization/review churn.",
      "items": {
        "type": "object",
        "required": ["task_id", "input"],
        "additionalProperties": false,
        "properties": {
          "task_id": { "type": "string", "description": "Stable id, replayable across sessions." },
          "input": { "type": "string", "description": "The exact skill input/args for this task." },
          "gold": {
            "type": "string",
            "description": "Gold/human expectation, when one exists — graded FIRST (§17.5). Optional; some skills only have a rubric."
          },
          "notes": { "type": "string" }
        }
      }
    }
  }
}
