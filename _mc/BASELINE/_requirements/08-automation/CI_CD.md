# [Project Name] — CI/CD Pipeline

<!-- GUIDANCE: Automated checks and deployments. Define what runs on every PR, every merge, and every deploy. -->

## PR Checks (runs on every pull request)

<!-- GUIDANCE: What must pass before a PR can merge?
- [ ] TypeScript compilation (tsc --noEmit)
- [ ] Lint (eslint)
- [ ] Unit tests
- [ ] E2E tests (if applicable)
- [ ] Security scan
- [ ] Build succeeds
-->

## Merge Pipeline (runs on merge to main)

<!-- GUIDANCE: What happens after merge? Auto-deploy to staging? Run full test suite? Build artifacts? -->

## Deploy Pipeline (runs on production deploy)

<!-- GUIDANCE: Pre-deploy checks, deploy steps, post-deploy verification, auto-rollback triggers. -->

## Scheduled Jobs

<!-- GUIDANCE: Cron jobs, scheduled tasks, periodic maintenance. What runs, when, what it does. -->
