import type { Metadata } from "next";
import { ACTIVATION_DEFINITION } from "@/lib/telemetry/events";
import "./globals.css";

export const metadata: Metadata = {
  title: "{{SLUG}}",
  description: "Scaffolded with MC - Next.js App Router + Tailwind v4 + shadcn/ui.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body
        className="min-h-screen bg-background text-foreground antialiased"
        data-activation-provenance={ACTIVATION_DEFINITION.provenance}
      >
        {children}
      </body>
    </html>
  );
}
