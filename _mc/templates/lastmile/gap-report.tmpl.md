# {{title}}

> Generated by `/bootstrap:lastmile` on {{generated_at}}. {{summary}}
>
> A snapshot of what blocks a paid/public launch right now. Re-run
> `/bootstrap:lastmile --phase audit` after changes to refresh the score.

{{body}}

---

_Produced by MC `bootstrap:lastmile`. The Launch-Readiness Score is honest
scaffold — a low number names the work, it does not hide it. Human-approval gates
are never auto-cleared; sensitive-data findings require legal/security review._
