# {{title}}

> Generated by `/bootstrap:lastmile` on {{generated_at}}. {{summary}}
>
> The shortest *safe* path to a paid launch. Each module section is sprint-ready;
> inject them via `/bootstrap:lastmile --phase inject`.

{{body}}

---

_Produced by MC `bootstrap:lastmile`. Rule: do not overbuild — anything
beyond the named gaps is deferred unless a concrete, evidence-backed need
justifies it. Human-approval gates listed above must be cleared by the operator._
