# {{title}}

> Generated by `/bootstrap:lastmile` on {{generated_at}}. {{summary}}
>
> **Boring + reliable** beats clever infra for a launch: deploy previews, a
> rollback plan, and a real production smoke test ('it builds' ≠ 'it serves').

{{body}}

---

**Human-approval gates:** custom **domain / DNS** changes and **app-store
submission** are not auto-executed — they require explicit operator approval.
Always run the production smoke test (live URL returns 200 + the core path works)
after deploy.

_Produced by MC `bootstrap:lastmile`._
