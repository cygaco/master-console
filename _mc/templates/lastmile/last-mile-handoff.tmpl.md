# {{title}}

> Generated by `/bootstrap:lastmile` on {{generated_at}}. {{summary}}
>
> What's done, what's pending, and which human-approval gates remain before this
> product can launch and take money.

{{body}}

---

_Produced by MC `bootstrap:lastmile`. This handoff is a status snapshot, not a
launch authorization — every pending approval gate above must be cleared by a
human. Re-run `/bootstrap:lastmile --phase audit` to refresh the readiness score._
