---
title: Product context questionnaire — {{slug}}
slug: {{slug}}
generated_at: {{generated_at}}
section_set: {{section_set}}
generator: mc/product:import@0.1.0
---

# Product context questionnaire — {{slug}}

> Generated by `/product:import` on {{generated_at}} from the project at `{{project_root_label}}`.
> This questionnaire is the input for `/product:bootstrap`. Answer each section below; an operator will run the answers back through `--parse` to mint a structured product brief.

{{preamble}}

---

## How to use this file

Each section below has:
- A stable `## NN — Title` heading.
- A stable HTML-comment anchor `<!-- section: <id> -->` (this is how the answers are parsed back — keep it verbatim).
- A short "What we're after:" line that frames the question.
- The question itself.
- A response-format hint.

Answer underneath each anchor. Plain Markdown is fine.

{{sections}}

---

## How to reply

Please reply with **one Markdown file**. For each section above, keep the `## NN — Title` heading **and** the `<!-- section: <id> -->` anchor **verbatim**, then write your answer underneath.

The literal anchor comment is how this questionnaire is parsed when the answers come back — if it is missing, your section will be dropped.

Plain Markdown is fine. No need for code fences. If you have nothing to say for a section, leave the heading and anchor in place and write exactly:

`_skipped — operator declined to answer this section._`

When you are done, paste the entire reply back into a file in the originating project and run:

`node scripts/product/import.js --parse <path-to-pasted.md> --slug {{slug}}`

That command converts the markdown into `_docs/imports/{{slug}}/{{slug}}.answers.json`, which `/product:bootstrap --answers-file` consumes directly.
