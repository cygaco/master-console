{
  "version": 1,
  "generated_from": "authoritative — hand-edited; code + docs derive from this",
  "note": "Single source of truth for step order and phase membership for the product's core golden path(s). /maps:steps regenerates step tables in PRODUCT_MODEL.md / GLOSSARY.md / GOLDEN_PATHS.md from this file. The canon engine seeds phases + steps from the intent's golden paths; refine by hand.",
  "phases": {{phases}},
  "steps": {{steps}}
}
