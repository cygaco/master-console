{
  "version": 1,
  "description": "Single source of truth for values that appear in multiple files. When a value here changes, all appears_in files are flagged STALE. The canon engine seeds this from the product intent; add product-specific value groups (costs, thresholds, limits, enums) as keys, each entry { value, unit, appears_in: [] }.",
  "{{value_group_1_name}}": {{value_group_1}},
  "enums": {{enums}},
  "validation": {{validation_rules}}
}
