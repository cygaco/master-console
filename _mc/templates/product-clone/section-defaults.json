{
  "$schema": "mc/product-clone/section-defaults/v1",
  "_notes": "Default skeleton bodies the generator falls back to when an extraction pass returns no draft. Each value is markdown rendered verbatim under the section heading. The literal substring `{{gap_pass}}` is replaced with the extraction pass name (e.g. 'jtbds') before render so the partial-deliverable [GAP] marker in COPY C-11 is consistent.",
  "product_identity": "[GAP — identify — no product identity resolved: see Source Attribution Log for inputs]\n",
  "jtbds": "[GAP — extraction — no signal in source material for {{gap_pass}}: see Source Attribution Log for inputs]\n",
  "feature_list": "[GAP — extraction — no signal in source material for {{gap_pass}}: see Source Attribution Log for inputs]\n",
  "voice_of_customer": "[GAP — extraction — no signal in source material for {{gap_pass}}: see Source Attribution Log for inputs]\n",
  "gaps": "[GAP — extraction — no signal in source material for {{gap_pass}}: see Source Attribution Log for inputs]\n",
  "opportunities": "[GAP — extraction — no signal in source material for {{gap_pass}}: see Source Attribution Log for inputs]\n",
  "source_attribution_log": "_No sources fetched in this run._\n"
}
