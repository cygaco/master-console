# Customized Update Fixture

This fixture represents a pre-0.1.0 install where `CLAUDE.md` was locally
customized. `/mc:update --dry-run` should classify customized framework docs as
review/merge work instead of overwriting them silently.
